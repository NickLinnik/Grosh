[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "grosh-ingestion"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
    "asyncpg>=0.29",
    "confluent-kafka>=2.4",
    "PyJWT>=2.8",
    "httpx>=0.27",
    "pydantic[email]>=2.7",
    "grosh-shared",
    "kubernetes>=29.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "ruff>=0.4",
    "mypy>=1.10",
]

[tool.uv.sources]
grosh-shared = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/grosh_ingestion"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true

[[tool.mypy.overrides]]
module = ["kubernetes.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"
