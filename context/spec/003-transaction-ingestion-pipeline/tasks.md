# Tasks: Transaction Ingestion Pipeline

---

## Slice 1: Database foundation — extensions, tables, hypertable, aggregates, currency rates

- [x] Create migration `0004_transaction_pipeline.py` — enable `timescaledb` and `pgcrypto` extensions, create enums (`bank_source`, `account_type`, `transaction_type`, `transaction_source`, `transaction_origin`), create `bank_integrations`, `accounts`, `categories`, `transactions` tables with all columns per tech spec (including `amount_uah_cents`, `amount_usd_cents`, `amount_eur_cents`, `metadata JSONB`, `origin`, `related_transaction_id`). Convert `transactions` to hypertable. Create `currency_rates` SCD2 table. Add all indexes. Enable RLS + policies on all user-scoped tables. **[Agent: postgres-database]**
- [x] Create migration `0005_monthly_aggregates.py` — continuous aggregate over denormalized per-currency amounts (income/expense/delta for UAH, USD, EUR) plus per-currency NULL conversion counts (`null_uah_count`, `null_usd_count`, `null_eur_count`) for data quality visibility. Real-time aggregation + refresh policy. **[Agent: postgres-database]**
- [x] Apply migrations (`upgrade head`). Verify all tables, indexes, RLS policies, hypertable, and continuous aggregate exist. Insert test rates + transactions, confirm aggregate correctness. **[Agent: postgres-database]**

---

## Slice 2: Shared models + Redpanda wire format + ID utils

- [x] Create `shared/src/grosh_shared/events.py` with `RawTransactionEvent` and `BackfillRequestEvent` Pydantic models. **[Agent: python-backend]**
- [x] Create `shared/src/grosh_shared/id_utils.py` with `generate_transaction_id(source, source_id) -> UUID` using UUID5. **[Agent: python-backend]**
- [x] Update `shared/src/grosh_shared/models.py` — update `Transaction` model, add `Account`, `BankIntegration` domain models, `TransactionOrigin` enum. **[Agent: python-backend]**
- [x] Create Monobank HTTP client — httpx-based async client with `get_client_info`, `set_webhook`, `get_statements` methods. Now at `banks/monobank/client.py`. **[Agent: python-backend]**
- [x] Create `shared/src/grosh_shared/auth.py` — JWT decode/validate utility extracted from the API service, reusable by both API and ingestion service. **[Agent: python-backend]**
- [x] Verify — unit tests for event serialization round-trip, ID determinism (same input → same UUID), and Monobank client (mocked httpx responses). **[Agent: python-backend]**

---

## Slice 3: Ingestion service scaffold + Redpanda producer + webhook + currency rate cron

- [x] Scaffold `services/ingestion/` — `pyproject.toml`, `Dockerfile`, `src/grosh_ingestion/main.py` (app factory with lifespan: asyncpg pool + confluent_kafka Producer), `deps.py` (DI composition root with JWT validation dependency). Follow existing API service patterns. **[Agent: python-backend]**
- [x] Add ingestion service to `infra/docker-compose.yml` and `infra/docker-compose.dev.yml`. **[Agent: k8s-infra]**
- [x] Create `banks/monobank/adapter.py` — normalize Monobank payload to `RawTransactionEvent` (abs amounts, infer transaction_type from sign). **[Agent: python-backend]**
- [x] Create `banks/monobank/webhook.py` — GET (verification) and POST (parse payload, lookup webhook_secret in `bank_integrations`, normalize via adapter, publish to Redpanda). **[Agent: python-backend]**
- [x] Create `repositories/account_repo.py` — covers both `bank_integrations` and `accounts` tables (single aggregate root). Read operations: lookup integration by webhook_secret, lookup account by external_id + integration_id. Also create `user_repo.py` with `is_active` check. Refactor `deps.py` and `banks/monobank/webhook.py` to use repos instead of raw SQL. **[Agent: python-backend]**
- [x] Create currency rate ingestion — per-source background loops via `RateProviderConfig` (source, fetch, interval_seconds, kind). Monobank (`RateKind.POLLED`, 300s) and NBU (`RateKind.HISTORICAL`, daily). Polled sources use `upsert_polled` (SCD2 with `last_polled_at` + `update_cadence_seconds`); historical sources use `upsert_historical` (slot-in SCD2, no polling metadata). `NormalizedRate` carries provider-authoritative `at_time` (Monobank Unix timestamp, NBU parsed `DD.MM.YYYY`). **[Agent: python-backend]**
- [x] Verify — start the stack with `make dev`, confirm ingestion service is healthy. `curl` the GET webhook endpoint (200 OK). `curl` POST with a mock Monobank payload, confirm message arrives on `raw_transactions` topic via `rpk topic consume`. Confirm currency rates are being populated. **[Agent: python-backend]**
- [x] Review fixes — `TransactionType.check` for zero-amount transactions, parameterized RLS set_config, extracted `CurrencyRateService` with dispatch by `RateKind`, webhook error handling (ValidationError + ValueError), Kafka delivery callback, typed repo returns (`IntegrationRef`/`AccountRef`), NUMERIC(18,8) for rates, adapter + webhook unit tests (14 new tests). **[Agent: python-backend]**

---

## Slice 4: Currency rate fallback chain + two-tier rate resolution

- [x] Create migration `0005_rate_source_config.py` — add `last_polled_at TIMESTAMPTZ` and `update_cadence_seconds INTEGER` to `currency_rates`, create `rate_source_config` table (source PK, fallback_source FK, base_currencies TEXT[]) with seed data (nbu→NULL `{UAH}`, monobank→nbu `{UAH}`). **[Agent: postgres-database]**
- [x] Split `repositories/currency_rate_repo.py` into `upsert_polled` (SCD2 anchored at `at_time`, bumps `last_polled_at`, carries `update_cadence_seconds`, `FOR UPDATE` locking) and `upsert_historical` (slot-in SCD2 with explicit `at_time` as `valid_from`, advisory lock, no polling metadata). **[Agent: python-backend]**
- [x] Add historical rate fetching to `banks/nbu/` — new `NbuHistoricalRate` model in `client.py` (different schema from daily: includes `units`, `rate_per_unit`, `enname`, `group`, `calcdate`). New `fetch_nbu_historical_rates(from_date, to_date, valcode)` in `client.py` using `https://bank.gov.ua/NBU_Exchange/exchange_site?start=YYYYMMDD&end=YYYYMMDD&valcode=CC&json`. New `fetch_historical_rates(from_date, to_date)` in `rates_provider.py` that loops all currencies (~45 requests) and normalizes to `list[NormalizedRate]`. Use `rate_per_unit` (not `rate`) for conversion. **[Agent: python-backend]**
- [x] Verify — apply migration, run rate loop twice, confirm `last_polled_at` updates on unchanged rates (no new rows). Call `fetch_historical_rates(date(2025, 1, 1), date(2025, 1, 31))`, confirm rates returned for the full range. **[Agent: python-backend]**
- [x] Implement unit and integration tests per `ingestion-currency-rate-test-suite.md` — provider unit tests (Monobank, NBU live, NBU historical), service dispatch tests, rate loop tests, repo integration tests (`upsert_polled`, `upsert_historical`), end-to-end service tests. Target ≥90% line / ≥85% branch coverage. **[Agent: python-backend]**

---

## Slice 5: Transaction consumer — dedup + transfer detection + currency conversion + DB write

- [x] Create `services/consumer/src/grosh_consumer/db.py` — asyncpg pool setup. **[Agent: python-backend]**
- [x] Create `services/consumer/src/grosh_consumer/handlers/transaction_handler.py` — dedup via `ON CONFLICT DO NOTHING`, transfer detection via counterparty IBAN lookup (overrides transaction_type to 'transfer' if match), two-tier rate lookup with fallback chain (FRESH via `last_polled_at + K * update_cadence_seconds >= T`, CLOSEST by proximity within 7 days), compute denormalized `amount_uah_cents`/`amount_usd_cents`/`amount_eur_cents`, write path-based rate traceability per currency to transaction `metadata` (`{"rate_eur": {"path": [{"from": "USD", "to": "EUR", ...}], "effective_rate": "0.9030"}}`). Hold flag is stored but not used for linking or filtering (Monobank historical API returns unreliable values). **[Agent: python-backend]**
- [x] Create `services/consumer/src/grosh_consumer/consumer.py` — main consumer loop: poll, deserialize, dispatch to handler, manual offset commit. Replace placeholder `main.py`. **[Agent: python-backend]**
- [x] Verify — publish a test event to `raw_transactions` via `rpk topic produce`, confirm it appears in the `transactions` table with correct denormalized amounts and rate source metadata. Publish the same event again, confirm no duplicate. Insert two accounts with known IBANs, publish a transfer-like event, confirm `transaction_type = 'transfer'`. Test fallback: insert a stale monobank rate, confirm consumer uses NBU instead. **[Agent: python-backend]**
- [x] Implement unit tests per `consumer-currency-conversion-test-suite.md` sections 1-3 — shared fixtures, `InMemoryRateRepo`, pure function tests (`_pick_rate`, `_ordered_pivots`, `_ensure_tz`, `RateTier`/`RateSide`, `_RateStep.apply`, `_RatePath`), service resolution tests with in-memory repo (tier ordering, source priority, multi-hop, rate-side, no-path, metadata shape). **[Agent: python-backend]**
- [x] Implement integration tests per `consumer-currency-conversion-test-suite.md` sections 4-5 — real DB with rolled-back transactions. Repo tests (`find_fresh_rate`, `find_closest_rate`, `load_source_chain` including depth cap, cyclic chain, base_currencies). End-to-end service tests (passthrough, direct/reverse/2-hop, tier coherence, ROUND_HALF_UP, large amounts, naive timezone, `RateSourceChainError` propagation). Target ≥95% line / ≥90% branch coverage. **[Agent: python-backend]**

---

## Slice 6: Account linking + manual entry (ingestion service)

- [x] Add generic write operations — `repositories/integration_repo.py` (`create_integration()` takes config dict, no bank-specific columns) and `repositories/account_repo.py` (`create_account()` with source + integration_id params, `get_user_default_rate_source()`). **[Agent: python-backend]**
- [x] Create `sources/monobank/linking_service.py` — `MonobankLinkingService`: calls Monobank `get_client_info`, generates `webhook_secret`, encrypts token via `pgp_sym_encrypt` (direct SQL call), stores encrypted bytes as hex in `config.encrypted_token`, creates integration via `IntegrationRepo`, creates accounts via `AccountRepo`, calls `set_webhook`. **[Agent: python-backend]**
- [x] Create `sources/monobank/repo.py` — `MonobankRepo`: `get_active_integration_by_webhook_secret()` (queries `config->>'webhook_secret'`), `get_account_by_external_id()`. **[Agent: python-backend]**
- [x] Create `sources/monobank/router.py` — `POST /monobank/link` (JWT), `GET /monobank/webhook/{secret}`, `POST /monobank/webhook/{secret}`. Register router in `main.py`. **[Agent: python-backend]**
- [x] Create `sources/manual/service.py` — `ManualService`: account creation, manual transaction entry (generate deterministic ID, build `RawTransactionEvent`, publish to Redpanda). **[Agent: python-backend]**
- [x] Create `sources/manual/router.py` — `POST /manual/accounts`, `POST /manual/transactions`. Register router in `main.py`. **[Agent: python-backend]**
- [x] Create migration `0006_user_settings.py` — `user_settings` table with `user_id UUID PK FK→users`, `default_rate_source TEXT FK→rate_source_config NULL` (nullable, no default), `updated_at TIMESTAMPTZ DEFAULT now()`. Enable RLS on `user_id`. Create `AFTER INSERT ON users` trigger to auto-create a row with NULL `default_rate_source` for each new user. **[Agent: postgres-database]**
- [x] Add `rate_source: str | None = None` to `RawTransactionEvent`. Bank `transaction_adapter.py` sets it to the bank source (e.g. `"monobank"`). `ManualService` resolves it from the request or `user_settings.default_rate_source`. **[Agent: python-backend]**
- [x] Update consumer `CurrencyConversionService.convert()` — use `event.rate_source or event.source` for `load_source_chain()` (backwards compatibility with events already on the topic). **[Agent: python-backend]**
- [x] Verify — call `POST /monobank/link` with a real or mocked Monobank token, confirm integration + accounts created in DB, token is encrypted, `config->>'webhook_secret'` is populated. Create a manual cash account via `POST /manual/accounts`. Post a manual transaction with explicit `rate_source`, confirm correct chain used. Post without `rate_source`, confirm `default_rate_source` from `user_settings` is used. Confirm bank webhook transactions still work (adapter sets `rate_source`). **[Agent: python-backend]**

---

## Slice 7: Transaction query endpoints (main API)

- [x] Create `services/api/src/grosh_api/repositories/account_repo.py` — read-only account listing. **[Agent: python-backend]**
- [x] Create `services/api/src/grosh_api/repositories/transaction_repo.py` — paginated transaction query with filters (type, account, date range), monthly aggregate query against the continuous aggregate view. **[Agent: python-backend]**
- [x] Create `services/api/src/grosh_api/routers/accounts.py` — `GET /accounts`. Register router in `main.py`. **[Agent: python-backend]**
- [x] Create `services/api/src/grosh_api/routers/transactions.py` — `GET /transactions` and `GET /transactions/monthly-aggregate`. Register router in `main.py`. **[Agent: python-backend]**
- [x] Verify — seed several transactions (income, expense, transfer) across 2+ months via the ingestion pipeline. Call `GET /transactions` with filters on the main API, confirm correct results. Call `GET /transactions/monthly-aggregate`, confirm all three currency views are correct and transfers excluded. Call `GET /accounts`, confirm accounts are listed. Clean up seeded test data from the DB after verification. **[Agent: python-backend]**
- [x] **Schema bugfix (post-slice)** — three corrections applied to the `transactions` table and wire format: (1) `operation_currency_code TEXT NULL` added — the merchant/operation currency for cross-currency purchases (e.g. `EUR`); NULL for domestic transactions. (2) `transaction_type` column renamed to `raw_transaction_type` — the immutable sign-based classification from the adapter. (3) New `transaction_type transaction_type NOT NULL` column added — the consumer-enriched classification, copied from `raw_transaction_type` by default and upgraded to `'transfer'` on detected internal transfers. Currency semantics clarified: `currency_code` = account base currency (resolved by consumer from DB); `amount_cents` = amount in the account's base currency. `RawTransactionEvent.currency_code` renamed to `operation_currency_code` on the wire. **[Agent: python-backend]**

---

## Slice 8: Backfill via K8s Jobs (transactions + rates)

- [x] Create `infra/k8s/transactions-backfill-job-template.yaml` — K8s Job manifest with ingestion service image, env var–based parameters, resource limits, backoffLimit, ttl. **[Agent: k8s-infra]**
- [x] Create `infra/k8s/rates-backfill-job-template.yaml` — K8s Job manifest for historical rate backfill. **[Agent: k8s-infra]**
- [x] Extend `models.py` with backfill interfaces — `HistoricalRateProvider` type alias + `fetch_historical` field on `RateProviderConfig`, `TransactionBackfillProvider` protocol. Add repo methods: `UserRepo.get_role()`, `MonobankRepo.decrypt_token()`, `IntegrationRepo.get_bank_source()`. **[Agent: python-backend]**
- [x] Create `sources/monobank/backfill.py` — `MonobankBackfillProvider` implementing `TransactionBackfillProvider`. Encapsulates token decryption (via `MonobankRepo`), account resolution, Monobank client, 31-day chunked pagination with 61s rate-limit pause, adapter normalization, Redpanda publish. No raw SQL. **[Agent: python-backend]**
- [x] Create `registry.py` — `RATE_PROVIDERS` list (with `fetch_historical` on NBU config) and `TRANSACTION_BACKFILL_PROVIDERS` dict. Imported by `main.py` and both backfill scripts. **[Agent: python-backend]**
- [x] Create source-agnostic backfill entrypoints — `jobs/run_transactions_backfill.py` reads params from env vars, resolves bank source via `IntegrationRepo.get_bank_source()`, dispatches to `TRANSACTION_BACKFILL_PROVIDERS`. `jobs/run_rates_backfill.py` looks up `RateProviderConfig` from registry, calls `config.fetch_historical()`. No source names in either file. **[Agent: python-backend]**
- [x] Create `services/backfill_service.py` — generic K8s Job creation via `kubernetes` Python client. `trigger_transactions_backfill` passes all params as env vars (no Redpanda topic). `trigger_rates_backfill` same pattern. **[Agent: python-backend]**
- [x] Add `POST /monobank/accounts/{account_id}/backfill` to Monobank router (transactions backfill trigger). **[Agent: python-backend]**
- [x] Create `routers/admin.py` — `POST /admin/rates-backfill` triggers rates backfill K8s Job. Admin role check via `UserRepo.get_role()`. Source validation via registry lookup. **[Agent: python-backend]**
- [x] Set up RBAC — ServiceAccount + Role + RoleBinding for ingestion pod to create Jobs. **[Agent: k8s-infra]**
- [x] Update `main.py` — import `RATE_PROVIDERS` from `registry`, admin router from `routers.admin`. Remove `BackfillRequestEvent` and `Topic.backfill_requests` from shared package. **[Agent: python-backend]**
- [x] Create `infra/grosh.postman_collection.json` — Postman collection covering all API and ingestion endpoints. Auto-token management via Login test script. **[Agent: python-backend]**
- [x] Create `services/api/src/grosh_api/repositories/rate_repo.py` — read-only rate queries: cursor-paginated list with filters (source, currency_from, currency_to, date range) and point-in-time SCD2 lookup. **[Agent: python-backend]**
- [x] Create `services/api/src/grosh_api/routers/rates.py` — `GET /rates` (paginated, filtered) and `GET /rates/at` (point-in-time rates). Register router in `main.py`. Update Postman collection. **[Agent: python-backend]**
- [x] Create `POST /monobank/relink` — re-registers webhook and updates token on existing integration without recreating accounts. **[Agent: python-backend]**
- [x] Create `WebhookReregistrationProvider` protocol and `WEBHOOK_REREGISTRATION_PROVIDERS` registry. Monobank implements via `MonobankLinkingService.reregister_webhooks()`. **[Agent: python-backend]**
- [x] Create `jobs/reregister_webhooks.py` — iterates all providers in registry. Shell entrypoints: `scripts/reregister-webhooks/dev.sh` (docker compose exec) and `scripts/reregister-webhooks/prod.sh` (kubectl exec). Makefile target: `make dev-reregister-webhooks`. **[Agent: python-backend]**
- [x] Verify — trigger transactions backfill from Monobank router, confirm K8s Job created with env vars. Trigger rates backfill via admin endpoint for NBU. Call `GET /rates/at` to confirm rate counts. Confirm no source names in generic files. **[Agent: python-backend]**

---

## Slice 9: Per-service database roles + account endpoint migration

**Design:** Four roles — `grosh_admin` (owner, migrations only), `grosh_api`, `grosh_ingestion`, `grosh_consumer`. All three app roles get `SELECT ON ALL TABLES`. Write privileges are table-specific: no table has write access from more than one service. Account management endpoints (PUT, DELETE) move from the API service to the ingestion service so `accounts` writes are owned entirely by ingestion.

- [x] Create migration `0007_app_roles.py` — create `grosh_api`, `grosh_ingestion`, `grosh_consumer` roles with `LOGIN`. `grosh_consumer` additionally gets `BYPASSRLS`. Grant `CONNECT`, `USAGE ON SCHEMA public`, `SELECT ON ALL TABLES`, `USAGE/SELECT ON ALL SEQUENCES` to all three. Table-specific writes: `grosh_api` gets INSERT+UPDATE on `users`, `user_settings`; INSERT+DELETE on `refresh_tokens`. `grosh_ingestion` gets INSERT+UPDATE on `accounts`, `bank_integrations`, `currency_rates`. `grosh_consumer` gets INSERT on `transactions`. `ALTER DEFAULT PRIVILEGES FOR ROLE grosh_admin` grants SELECT + sequence usage to all three. Passwords from env vars `GROSH_API_DB_PASSWORD`, `GROSH_INGESTION_DB_PASSWORD`, `GROSH_CONSUMER_DB_PASSWORD`. **[Agent: postgres-database]**
- [x] Update `infra/.env` and `infra/docker-compose.yml` — add three password env vars. Each service gets its own `DATABASE_URL` using its role. Add `DATABASE_URL_ADMIN` for Alembic. **[Agent: k8s-infra]**
- [x] Move account write endpoints to ingestion — move `PUT /accounts/{id}` (rename) and `DELETE /accounts/{id}` (soft-delete) from `services/api/src/grosh_api/routers/accounts.py` to `services/ingestion/src/grosh_ingestion/sources/manual/router.py` as `PUT /manual/accounts/{id}` and `DELETE /manual/accounts/{id}`. Move `update_name()` and `soft_delete()` repo methods from API's `account_repo.py` to ingestion's `account_repo.py`. Remove write methods and endpoints from API service. **[Agent: python-backend]**
- [x] Update API service — use `DATABASE_URL` with `grosh_api` role. Remove `UpdateAccountRequest` schema and write-related imports from accounts router. Verify `get_current_user` dependency still calls `set_config`. **[Agent: python-backend]**
- [x] Update ingestion service — use `DATABASE_URL` with `grosh_ingestion` role. Verify `set_config` is called. Currency rate writes (global tables, no RLS) must still work. **[Agent: python-backend]**
- [x] Update consumer service — use `DATABASE_URL` with `grosh_consumer` role (has `BYPASSRLS`, so RLS policies don't apply). No `set_config` needed. **[Agent: python-backend]**
- [x] Update Alembic `env.py` — use `DATABASE_URL_ADMIN` for migrations. **[Agent: python-backend]**
- [x] Update Postman collection — move account PUT/DELETE requests from API folder to Ingestion folder with updated paths. **[Agent: python-backend]**
- [x] Verify — start the stack with `make dev`. Check `pg_stat_activity`: API connects as `grosh_api`, ingestion as `grosh_ingestion`, consumer as `grosh_consumer`. Test: API cannot INSERT into `transactions` (permission denied). Consumer can read all accounts without `set_config`. Ingestion can write accounts but not transactions. Full webhook → consumer → DB pipeline works. Account rename and soft-delete work via ingestion endpoints. **[Agent: python-backend]**

---

## Slice 10: Access token revocation (instant logout)

**Design:** `revoked_tokens` TimescaleDB hypertable with 1-hour retention policy. On logout, the access token's `jti` is inserted. The `get_current_user` dependency checks revocation before proceeding. TimescaleDB auto-drops expired entries.

- [x] Create migration `0008_revoked_tokens.py` — create `revoked_tokens` table (`jti UUID NOT NULL`, `expires_at TIMESTAMPTZ NOT NULL`), convert to hypertable on `expires_at`, add retention policy (1 hour). Grant INSERT, DELETE to `grosh_api`. **[Agent: postgres-database]**
- [x] Add `RevokedTokenRepo` to API service — `insert(conn, jti, expires_at)` and `is_revoked(conn, jti) -> bool`. **[Agent: python-backend]**
- [x] Update `get_current_user` dependency — after decoding JWT, call `repo.is_revoked(conn, jti)`. If revoked, raise 401. Extract `jti` from the JWT payload (already present as a standard claim). **[Agent: python-backend]**
- [x] Update `AuthService.logout()` — in addition to deleting the refresh token, insert the access token's `jti` + `exp` into `revoked_tokens`. The access token must be passed (from `Authorization` header) so its `jti` can be extracted. **[Agent: python-backend]**
- [x] Update `AuthService.logout_all()` — revoke the current access token (same as above). All refresh tokens are already deleted. **[Agent: python-backend]**
- [x] Update logout router endpoints — pass the access token (from request header) to the service so it can extract `jti`. **[Agent: python-backend]**
- [x] Verify — login, hit an authenticated endpoint (works), logout, hit the same endpoint again (401). Refresh after logout also fails (refresh token deleted). Wait 15+ minutes after logout, confirm `revoked_tokens` row is cleaned up by retention policy. **[Agent: python-backend]**

---

## Slice 11: Drop TimescaleDB — plain PostgreSQL + pg_cron

**Design:** Remove all TimescaleDB usage. `transactions` becomes a regular table with `PRIMARY KEY (id)`. `revoked_tokens` switches from hypertable+retention to plain table+pg_cron. `monthly_aggregates` continuous aggregate is deleted entirely (replaced by compute-on-read in a later slice). Docker image switches from `timescale/timescaledb-ha:pg16` (5.4 GB) to custom `pgvector/pgvector:pg16` + `pg_cron` (~450 MB). Full rationale in `references/adr-drop-timescaledb.md`.

**Local data preservation:** The local dev DB has ~40 months of backfilled data. Before applying migration changes: verify no duplicate IDs (`SELECT id, COUNT(*) FROM transactions GROUP BY id HAVING COUNT(*) > 1`), dump data tables (`pg_dump --data-only --format=plain`), nuke the volume (`docker compose down -v`), apply fresh migrations, restore from dump.

- [x] Modify migration `0004_transaction_pipeline.py` — remove `CREATE EXTENSION IF NOT EXISTS timescaledb`, remove `SELECT create_hypertable('transactions', 'time')`, change PK from `(id, time)` to `(id)`, change dedup index from `UNIQUE (account_id, source_id, time)` to `UNIQUE (account_id, source_id)`, add `related_transaction_id UUID REFERENCES transactions(id) ON DELETE SET NULL`, add `rate_source TEXT` column (nullable, defaults to NULL — no backfill needed; consumer handles NULL via `event.rate_source or event.source` fallback). **[Agent: postgres-database]**
- [x] Delete migration `0005_monthly_aggregates.py` entirely. Update `0005_rate_source_config.py`'s `down_revision` from `"0005"` to `"0004"`. **[Agent: postgres-database]**
- [x] Modify migration `0008_revoked_tokens.py` — remove `SELECT create_hypertable(...)` and `SELECT add_retention_policy(...)`. Add `CREATE EXTENSION IF NOT EXISTS pg_cron`. Add `SELECT cron.schedule_in_database('purge-revoked-tokens', '*/5 * * * *', $$DELETE FROM revoked_tokens WHERE expires_at < now()$$, 'grosh')`. Add explicit `PRIMARY KEY (jti)`. Add `CREATE INDEX idx_revoked_tokens_expires_at ON revoked_tokens (expires_at)`. **[Agent: postgres-database]**
- [x] Create custom PostgreSQL Dockerfile — `FROM pgvector/pgvector:pg16`, install `postgresql-16-cron`. Update `infra/docker-compose.yml`: switch image, add `command: postgres -c shared_preload_libraries=pg_cron -c cron.database_name=grosh`. **[Agent: k8s-infra]**
- [x] Update consumer `transaction_repo.py` — change `ON CONFLICT (id, time) DO NOTHING` to `ON CONFLICT (id) DO NOTHING`. **[Agent: python-backend]**
- [x] Update API `transaction_repo.py` — remove any references to composite PK or `monthly_aggregates` view. Remove `MonthlyAggregateResponse` model and `GET /transactions/monthly-aggregate` endpoint from `transactions.py` router (will be replaced by aggregation API in a later slice). **[Agent: python-backend]**
- [x] Verify — nuke Docker volume, run `alembic upgrade head` on fresh DB, confirm all tables exist with correct PK structure. Insert test transactions, confirm `ON CONFLICT (id) DO NOTHING` works. Insert expired revoked token, wait 5 min, confirm pg_cron purged it (`SELECT * FROM cron.job` shows the job; `SELECT * FROM revoked_tokens` is empty after purge). Confirm `related_transaction_id` FK works (insert pair, delete one, confirm SET NULL). **[Agent: postgres-database]**

---

## Slice 12: Add missing schema — transfer detection indexes, anomaly table, user_settings timezone, reprocessing tables

**Design:** Add all schema objects required by transfer detection, reprocessing, and timezone-aware aggregation ADRs. These are added to existing unmerged migrations (migration hygiene rule: don't create new files for unmerged-branch changes).

- [x] Add to migration `0004_transaction_pipeline.py` — create `transfer_anomaly_reason` enum type (7 values), create `transfer_match_anomalies` table (`id UUID PK`, `transaction_id UUID FK UNIQUE`, `candidate_ids UUID[]`, `reason_code`, `reason_detail TEXT`, `created_at`). Add 3 transfer detection indexes: Tier A (`user_id, account_id, raw_transaction_type, time DESC WHERE mcc=4829 AND related_transaction_id IS NULL`), Tier B (`user_id, counterparty_iban, raw_transaction_type, time DESC WHERE mcc=4829 AND related_transaction_id IS NULL AND counterparty_iban IS NOT NULL`), Tier C (`user_id, operation_amount_cents, raw_transaction_type, time DESC WHERE mcc=4829 AND counterparty_iban IS NULL AND related_transaction_id IS NULL`). Grant INSERT, DELETE on `transfer_match_anomalies` to `grosh_consumer` in migration `0007_app_roles.py`. **[Agent: postgres-database]**
- [x] Add to migration `0004_transaction_pipeline.py` — create `reprocessing_locks` table (`user_id UUID PK FK→users`, `locked_at TIMESTAMPTZ DEFAULT now()`), create `reprocessing_backups` table (`id UUID PK`, `user_id UUID FK→users`, `data JSONB NOT NULL`, `created_at`). Grant INSERT, DELETE on both to `grosh_consumer` in `0007_app_roles.py`. Also grant UPDATE, DELETE on `transactions` to `grosh_consumer` (needed for transfer pairing and reprocessing). **[Agent: postgres-database]**
- [x] Add `timezone TEXT NOT NULL DEFAULT 'UTC'` column to `user_settings` in migration `0006_user_settings.py`. **[Agent: postgres-database]**
- [x] Update API `PUT /settings` endpoint — accept optional `timezone` field, validate it's a valid IANA timezone identifier (use `zoneinfo.ZoneInfo(tz)` — raises `KeyError` on invalid). Return `timezone` in GET/PUT responses. **[Agent: python-backend]**
- [x] Verify — run `alembic upgrade head` on fresh DB. Confirm `transfer_match_anomalies` table exists with correct FK and UNIQUE constraint. Confirm `reprocessing_locks` and `reprocessing_backups` exist. Confirm `user_settings` has `timezone` column with default `'UTC'`. Test `PUT /settings { "timezone": "Europe/Kyiv" }` (200), `PUT /settings { "timezone": "Invalid/Zone" }` (422). **[Agent: postgres-database]**

---

## Slice 13: Two-stage consumer architecture — normalization consumer + pipeline consumer

**Design:** Refactor from single-stage consumer (subscribes to `raw_transactions`, processes `RawTransactionEvent`) to two-stage architecture per `references/adr-consumer-pipeline-architecture.md`. Stage 1 (normalization consumer) subscribes to per-source `raw_transactions.*` topics, dispatches to per-source `NormalizationStrategy`, publishes `NormalizedTransaction` to `normalized_transactions`. Stage 2 (pipeline consumer) subscribes to `normalized_transactions`, runs the layered pipeline. Ingestion switches from publishing normalized `RawTransactionEvent` to publishing raw bank payloads wrapped in `TransactionEnvelope`.

- [x] Create consumer models — `models/envelope.py` (`TransactionEnvelope`: user_id, account_id, source, payload dict), `models/normalized.py` (`NormalizedTransaction`: all fields from current `RawTransactionEvent` + `rate_source`). **[Agent: python-backend]**
- [x] Create normalization strategies — `sources/monobank/normalizer.py` (`MonobankNormalizer`: deserialize `MonobankStatementItem` from envelope payload, apply abs amounts, type inference from sign, currency code conversion via `iso_4217`, build `NormalizedTransaction`), `sources/monobank/models.py` (`MonobankStatementItem` Pydantic model — move from ingestion's `sources/monobank/models.py`), `sources/manual/normalizer.py` (`ManualNormalizer`: trivial — manual payload is already canonical). Register in strategy dict. **[Agent: python-backend]**
- [x] Create normalization consumer — `consumers/normalization_consumer.py`: subscribe to `raw_transactions.monobank` + `raw_transactions.manual`, deserialize `TransactionEnvelope`, dispatch to normalizer by `envelope.source`, publish `NormalizedTransaction` to `normalized_transactions`, manual offset commit. `group.id = normalization`. **[Agent: python-backend]**
- [x] Refactor pipeline consumer — rename/refactor current `consumer.py` + `transaction_handler.py` into `consumers/pipeline_consumer.py`. Subscribe to `normalized_transactions` instead of `raw_transactions`. Accept `NormalizedTransaction` instead of `RawTransactionEvent`. Keep existing currency conversion logic. Transfer detection remains IBAN-only for now (will be replaced in Slice 14). `group.id = transaction-pipeline`. **[Agent: python-backend]**
- [x] Create pipeline orchestrator — `services/pipeline.py`: defines the layer sequence (transfer detection → currency conversion → classification → persistence). Each layer has a Strategy protocol. Transfer detection dispatches per-source via registry. Currency conversion is source-agnostic. Classification is a no-op pass-through for now. **[Agent: python-backend]**
- [x] Update consumer entrypoint — `main.py` starts both consumer loops (normalization + pipeline) in a single process. **[Agent: python-backend]**
- [x] Update ingestion — Monobank webhook handler (`sources/monobank/router.py`): stop calling `to_raw_transaction_event()`, instead wrap the raw `statement_item` dict + routing fields (user_id, account_id, source) in a `TransactionEnvelope`, publish to `raw_transactions.monobank`. Delete `sources/monobank/transaction_adapter.py`. Update `sources/manual/service.py`: publish `TransactionEnvelope` to `raw_transactions.manual` instead of `RawTransactionEvent` to `raw_transactions`. Update `sources/monobank/backfill.py`: same envelope change, publish to `raw_transactions.monobank`. **[Agent: python-backend]**
- [x] Update shared models — add per-source topic values to `Topic` enum (`raw_transactions_monobank`, `raw_transactions_manual`, `normalized_transactions`). Remove old `raw_transactions` value. Delete `shared/src/grosh_shared/events.py` (`RawTransactionEvent` retired) and `shared/tests/test_events.py`. Update all files that import `RawTransactionEvent`: `consumer/consumer.py`, `consumer/handlers/transaction_handler.py`, `consumer/repositories/transaction_repo.py`, `consumer/services/currency_conversion_service.py`, `consumer/tests/helpers.py`, `ingestion/sources/manual/service.py`, `ingestion/sources/monobank/transaction_adapter.py` (deleted in same slice). **[Agent: python-backend]**
- [x] Verify — cutover follows the ADR's drain-and-switch procedure: pause ingestion writes briefly, wait for consumer to drain old `raw_transactions` topic (seconds at current volume), deploy new architecture (both consumers + new topics), deploy ingestion changes (publish to per-source topics), resume. After cutover: start the full stack with `make dev`. Trigger a Monobank webhook (or mock via `rpk topic produce raw_transactions.monobank`). Confirm message flows: raw topic → normalization consumer → normalized_transactions topic → pipeline consumer → DB. Confirm manual transaction entry flows through `raw_transactions.manual`. Confirm existing currency conversion still works. Confirm dedup still works (publish same event twice, only one row in DB). **[Agent: python-backend]**

---

## Slice 14: 3-tier transfer detection (Monobank strategy)

**Design:** Replace IBAN-only transfer detection with the full 3-tier algorithm per `references/adr-transfer-detection.md`. Implements `MonobankTransferDetection` as a `TransferDetectionStrategy` registered in the pipeline consumer. Tier A (IBAN match), Tier B (reverse IBAN), Tier C (operation_amount cross-match + semantic description guard). Anomaly recording for rejected/ambiguous matches. Claim locks via `FOR UPDATE SKIP LOCKED`. Idempotency guard at service entry.

- [x] Create transfer detection protocol and result types — `services/transfer_detection.py` (`TransferDetectionStrategy` protocol, `TransferResult` dataclass with `transaction_type`, `related_transaction_id`, anomalies list). Register `TRANSFER_STRATEGIES: dict[str, TransferDetectionStrategy]`. **[Agent: python-backend]**
- [x] Create Monobank description guard — `sources/monobank/descriptions.py`: income descriptions ("З ...") mapped to source account constraints (type, currency), expense descriptions ("На ...") mapped to target account constraints. Include observed + speculative patterns per ADR tables. Validation function: given a description and an account, does the description match the account's properties? **[Agent: python-backend]**
- [x] Create `MonobankTransferDetection` strategy — `sources/monobank/transfer.py`: implements the 3-tier algorithm. Tier A: `counterparty_iban` → own account lookup → find unclaimed partner on target account (opposite type, ±2s, `FOR UPDATE SKIP LOCKED`). Tier B: reverse lookup (another unclaimed tx has `counterparty_iban = my IBAN`). Tier C: both NULL IBAN, `operation_amount_cents` cross-match, semantic description validation. Ambiguity rejection (>1 candidate → anomaly, no claim). Description validation on all tiers (canary on A/B, load-bearing on C). Idempotency check at entry (if `id` already in DB → return immediately). False negative detection (unpaired "З "/"На " descriptions → anomaly). **[Agent: python-backend]**
- [x] Create anomaly repository — `repositories/anomaly_repo.py`: `record_anomaly(conn, transaction_id, candidate_ids, reason_code, reason_detail)`, `delete_unpaired_anomalies_for_transactions(conn, tx_ids)` (auto-resolve only `unpaired_*` anomalies on successful claim; terminal anomalies persist). **[Agent: python-backend]**
- [x] Integrate into pipeline — update `services/pipeline.py` to call `TransferDetectionStrategy.detect_and_pair()` before currency conversion. Register `MonobankTransferDetection` in `TRANSFER_STRATEGIES["monobank"]`. The pipeline orchestrator looks up the strategy by `tx.source` and delegates. Sources with no strategy skip transfer detection. **[Agent: python-backend]**
- [x] Implement unit test suites per `references/consumer-transfer-detection-test-suite.md` §2-10 — description guard, TransferResult model, Tier A/B/C with in-memory repos, anomaly recording, idempotency guard, multi-hop chains, claim lock simulation (180 tests). **[Agent: python-backend]**
- [x] Implement integration test suites per `references/consumer-transfer-detection-test-suite.md` §11-16 — Tier A/B/C against real Postgres, anomaly lifecycle + CASCADE, `FOR UPDATE SKIP LOCKED` + advisory locks, full pipeline end-to-end. **[Agent: python-backend]**
---

## Slice 14b: Replace transaction_type with direction + special_category

**Design:** The old `raw_transaction_type` / `transaction_type` two-field system loses information — setting `transaction_type = 'transfer'` overwrites the direction (income/expense). Replace with two orthogonal fields: `direction` (immutable money flow: `income`, `expense`, `zero`) and `special_category` (pipeline enrichment: NULL for ordinary, `'transfer'` for internal movements). This preserves direction on transfer legs and enables future enrichment values (`cancellation`, `hold`) without information loss. Eliminates `raw_transaction_type` entirely.

- [x] Renumber migrations — close the 0005 gap left by TimescaleDB CAGG removal: rename 0006→0005, 0007→0006, 0008→0007, 0009→0008. Update `revision` and `down_revision` strings in each file to match new numbers. Update `technical-considerations.md` if it references migration numbers by name. **[Agent: python-backend]**
- [x] Update migration 0004 — replace `transaction_type` enum with `transaction_direction` enum (`income`, `expense`, `zero`) and `special_category` enum (`transfer`). Replace `raw_transaction_type transaction_type NOT NULL` and `transaction_type transaction_type NOT NULL` columns with `direction transaction_direction NOT NULL` and `special_category special_category NULL`. Update transfer detection indexes to use `direction` instead of `raw_transaction_type`. Update `claim_pair` SQL to set `special_category = 'transfer'` instead of `transaction_type = 'transfer'`. **[Agent: postgres-database]**
- [x] Update shared models — replace `TransactionType` enum with `TransactionDirection` (`income`, `expense`, `zero`) and `SpecialCategory` (`transfer`). Update all references in shared package. **[Agent: python-backend]**
- [x] Update consumer normalizers — `MonobankNormalizer` and `ManualNormalizer` set `direction` instead of `transaction_type`. Positive → `income`, negative → `expense`, zero → `zero`. **[Agent: python-backend]**
- [x] Update `NormalizedTransaction` model — replace `transaction_type: str` with `direction: str`. Add no `special_category` field (normalizers don't set it — pipeline does). **[Agent: python-backend]**
- [x] Update `TransferResult` — replace `transaction_type: str` with `special_category: str | None`. The detect_and_pair() method returns `special_category='transfer'` on successful pair, `None` otherwise. **[Agent: python-backend]**
- [x] Update `MonobankTransferDetection` — all references to `transaction_type` become `direction` (for pair matching: opposite direction). `_opposite_type()` renamed to `_opposite_direction()`. Claim sets `special_category = 'transfer'` not `transaction_type = 'transfer'`. **[Agent: python-backend]**
- [x] Update `TransferQueryRepo` — `claim_pair` SQL: `SET special_category = 'transfer'` instead of `SET transaction_type = 'transfer'`. Tier queries filter on `direction` instead of `raw_transaction_type`. **[Agent: python-backend]**
- [x] Update `TransactionRepo.insert()` — replace `raw_transaction_type` and `transaction_type` columns with `direction` and `special_category`. **[Agent: python-backend]**
- [x] Update `PipelineOrchestrator` — use `TransferResult.special_category` and `TransferResult.related_transaction_id` when calling `transaction_repo.insert()`. **[Agent: python-backend]**
- [x] Update consumer main.py and pipeline_consumer.py if any references to old field names. **[Agent: python-backend]**
- [x] Update all unit tests — `make_event()` helper, in-memory repos, all transfer detection tests, normalizer tests. Replace `transaction_type` with `direction`, add `special_category` assertions where needed. **[Agent: python-backend]**
- [x] Update all integration tests — same field name migration. Verify transfer detection still pairs correctly with new schema. **[Agent: python-backend]**
- [x] Update API service — `GET /transactions` filter param changes from `type` to `direction` + `special_category`. Transaction response model uses new field names. **[Agent: python-backend]**
- [x] Verify end-to-end — automated, agent-driven with periodic polling. Steps: (1) Wipe all existing transactions (`DELETE FROM transactions`; CASCADE clears anomalies and nullifies `related_transaction_id`). (2) For the linked Monobank integration, iterate over all accounts and trigger backfill from `2023-01-01` to now via the backfill API endpoint. (3) Poll K8s job status every 5 minutes (`kubectl get jobs -l app=backfill`) until all jobs complete — expect ~3-4 hours total due to Monobank rate limits (1 req/60s/account, max 31 days/request). (4) After all jobs succeed, run SQL verification queries against the database: (a) `SELECT COUNT(*) FROM transactions WHERE direction IS NULL` = 0; (b) all MCC 4829 paired transactions have `related_transaction_id` set, `special_category = 'transfer'`, `direction` preserved as `income`/`expense`; (c) external P2P have `special_category IS NULL`; (d) `transfer_match_anomalies` has only `unpaired_*` entries (for legs whose partner is on unlinked accounts), zero `ambiguous_*` or `description_*_mismatch`; (e) `SELECT COUNT(*) FROM transactions WHERE special_category = 'transfer' AND related_transaction_id IS NULL` = 0 (no orphaned transfer flags); (f) aggregation query with `WHERE special_category IS NULL` excludes all transfer legs. Report pass/fail with counts. **[Agent: python-backend]**
- [x] Upgrade to PostgreSQL 18 + switch `currency_rates.id` to UUID — update Dockerfile base from `pgvector/pgvector:pg16` to `pgvector/pgvector:pg18`, install `postgresql-18-cron` instead of `postgresql-16-cron`. In migration 0004: change `currency_rates` PK from `BIGSERIAL` to `UUID PRIMARY KEY DEFAULT uuidv7()`. In consumer `currency_conversion_service.py`: change `rate_id: int` to `rate_id: UUID`, update format strings. Update docker-compose postgres image reference. **Rate data preservation:** write a throwaway script that dumps existing `currency_rates` rows (minus `id`), applies fresh migration, re-inserts with new `uuidv7()` IDs. Monobank polled rates have irreplaceable SCD2 history (`last_polled_at` snapshots not re-fetchable from API). Existing `rate_id` values in transaction metadata JSONB become orphaned — acceptable, they're informational traceability not live FKs. **[Agent: k8s-infra]**
- [x] Switch MCC from INTEGER to TEXT — MCC codes are identifiers (ISO 18245), not numbers. Add `iso18245` to shared deps, create `shared/src/grosh_shared/mcc.py` with `MccCode` enum (values are `iso18245.MCC` objects, `.code` property returns the string). Update migration 0004: `mcc INTEGER` → `mcc TEXT`. Update partial indexes: `WHERE mcc = 4829` → `WHERE mcc = '4829'`. Update all Python types: `mcc: int | None` → `mcc: str | None` in NormalizedTransaction, ManualTransactionPayload, TransactionRow, TransactionResponse, manual router/service, test helpers. Monobank raw model keeps `mcc: int` (bank sends integer); normalizer converts via `str(item.mcc)`. Transfer detection uses `MccCode.WIRE_TRANSFER.code` instead of hardcoded `4829`. Transfer query repo: `AND mcc = 4829` → `AND mcc = '4829'`. **Data surgery on live DB:** `ALTER TABLE transactions ALTER COLUMN mcc TYPE TEXT USING mcc::text;` (lossless cast, no data loss). Then update migration to match. **[Agent: python-backend]**

---

## Slice 15: Aggregation API — compute-on-read endpoint

**Design:** Implement `GET /transactions/aggregates` per `references/adr-drop-timescaledb.md`. Single SQL query with `date_trunc($bucket, time AT TIME ZONE $user_tz)`, FILTER clauses for income/expense, `converted_pct` per currency. Replaces the deleted `GET /transactions/monthly-aggregate`.

- [x] Create aggregation repository method — `services/api/src/grosh_api/repositories/transaction_repo.py`: `get_aggregates(conn, user_id, currencies, from_dt, to_dt, bucket, user_tz)`. Builds a single SQL query with `date_trunc` + `AT TIME ZONE`, FILTER clauses for income/expense, `COUNT(amount_{c}_cents) / COUNT(*)` for `converted_pct`. All requested currencies computed in one query. WHERE clause always filters to `transaction_type IN ('income', 'expense')`. Uses `COALESCE($from, '-infinity'::timestamptz)` for unbounded ranges. **[Agent: python-backend]**
- [x] Create aggregation endpoint — `services/api/src/grosh_api/routers/transactions.py`: `GET /transactions/aggregates` with query params `currency` (comma-separated, default all), `from`/`to` (optional dates), `bucket` (day/week/month/quarter/year, default month), `fields` (income/expense/delta, default all). Response schema: `{ bucket, items: [{ period_start, currencies: { UAH: { total_income_cents, total_expense_cents, delta_cents, converted_pct } } }] }`. The `fields` param controls which values appear in JSON (query always computes all). Reads `user_settings.timezone` for bucketing. **[Agent: python-backend]**
- [x] Update Postman collection — add `GET /transactions/aggregates` request with example params. Remove old `GET /transactions/monthly-aggregate` if still present. **[Agent: python-backend]**
- [x] Verify — seed transactions across multiple months with mixed currencies. Call `GET /transactions/aggregates?bucket=month` — confirm monthly totals match manual calculation. Call with `bucket=week`, `bucket=quarter` — confirm correct bucketing. Call with `currency=UAH` — confirm only UAH columns in response. Call with `from=2025-06-01&to=2025-06-30` — confirm date filtering. Confirm transfers are excluded. Confirm `converted_pct` < 100% when some transactions have NULL amounts for a currency. Test with user timezone set to `Europe/Kyiv` — confirm a transaction at 23:30 UTC+3 on Jan 31 falls in January bucket. **[Agent: python-backend]**

---

## Slice 15b: Half-open time ranges + review fixes

**Design:** Slice 15 review surfaced an inconsistency between `list_transactions` (uses `time <= to`, inclusive) and `get_aggregates` (uses `time < to`, exclusive). Per the new convention in `CLAUDE.md` — half-open `[from, to)` ranges everywhere — unify both endpoints and the `list_rates` filter on the half-open form. Also harden `ZoneInfo(user_tz)` against malformed DB values and remove dead imports left in `routers/transactions.py`.

- [x] Make `list_transactions` upper bound exclusive — in `services/api/src/grosh_api/repositories/transaction_repo.py::_build_transaction_conditions`, change `time <= $N` to `time < $N`. Update the `to_time` parameter docstring/comment if any to call out half-open semantics. Update OpenAPI description on the corresponding `to` query param in `routers/transactions.py` to read "exclusive upper bound". **[Agent: python-backend]**
- [x] Make `list_rates` upper bound exclusive — in `services/api/src/grosh_api/repositories/rate_repo.py`, change `("valid_from <=", to_time)` to `("valid_from <", to_time)`. Confirm the SCD2 lookup at line 160 (`valid_to IS NULL OR valid_to > $1`) is already half-open and does not change. Update OpenAPI description on the `to` param in `routers/rates.py`. **[Agent: python-backend]**
- [x] Wrap `ZoneInfo(user_tz)` in `transaction_repo.get_aggregates` — `PUT /settings` already validates timezones, but a corrupt DB value would surface as a 500. Catch `(KeyError, ValueError, ZoneInfoNotFoundError)` and raise a descriptive `ValueError` (or a custom `InvalidUserTimezone` exception) so the router can map it to a 422 with a message naming the offending value. Add a short comment pointing at the write-path validator. **[Agent: python-backend]**
- [x] Remove unused imports — in `services/api/src/grosh_api/routers/transactions.py`, drop `Literal` and `ZoneInfo` (and any other dead names flagged by ruff). Run the project's lint command to confirm clean. **[Agent: python-backend]**
- [x] Document half-open semantics in OpenAPI — add `description=` to every `from`/`to`/`at` `Query(...)` declaration that takes a time-range bound. Use a consistent phrasing: `from` → "Inclusive lower bound (`time >= from`).", `to` → "Exclusive upper bound (`time < to`). Pass the start of the next period to include a full period — e.g. `to=2026-02-01T00:00:00Z` for all of January 2026.", `at` (rates point-in-time) → "Point-in-time SCD2 lookup; returns rows where `valid_from <= at AND (valid_to IS NULL OR valid_to > at)`." Apply to `routers/transactions.py` (list + aggregates), `routers/rates.py` (list + at). **[Agent: python-backend]**
- [x] Update Postman collection examples to half-open — in `infra/grosh.postman_collection.json`, replace any `to=YYYY-12-31T23:59:59Z`-style end-of-day fudges with the next-period start (e.g. `to=2026-01-01T00:00:00Z`). Apply to the Transactions list, Aggregates, and Rates list requests. The example values should reinforce the convention rather than contradict it. **[Agent: python-backend]**
- [x] Verify — call `GET /transactions?from=2026-01-01&to=2026-02-01` and `GET /transactions/aggregates?bucket=month&from=2026-01-01&to=2026-02-01`; confirm both return data for January only and never include a `2026-02-01 00:00:00` row. Call `GET /rates?from=2026-01-01&to=2026-02-01` and confirm the same. Force-set `user_settings.timezone='Not/AZone'` directly in the DB (bypassing the validator) and confirm `GET /transactions/aggregates` returns 422 with a descriptive message (not 500). Open the Swagger UI at `/docs` and confirm the `from`/`to`/`at` descriptions render with the half-open language. Run ruff/mypy and confirm no warnings remain in the touched files. **[Agent: python-backend]**

---

## Slice 15c: Aggregation API — review follow-ups

**Design:** Slice 15/15b post-merge review surfaced four real issues — a silent-skip correctness bug on `unconverted_currency`, two over-length lines, missing OpenAPI documentation that `period_start` is UTC-serialized, and (per `CLAUDE.md`) no integration tests for the new aggregation endpoint or the half-open boundary changes. Also a Postman audit found two pre-existing query-key drifts from the Slice 14b refactor (`type` → `direction`, `transaction_type` → `direction`); those have already been fixed but should be covered by the new tests so they cannot silently regress.

- [x] Fix silent-skip on invalid `unconverted_currency` — in `services/api/src/grosh_api/repositories/transaction_repo.py::_build_transaction_conditions` (around line 137), the current code silently ignores unknown currency values via `_UNCONVERTED_CURRENCY_COLUMNS.get(...)` returning `None`. Per `CLAUDE.md` "Errors Must Be Explicit and Loud", invalid values must raise. Move validation to the router layer (`routers/transactions.py::list_transactions`): validate `unconverted_currency` against `{UAH, USD, EUR}` and raise `HTTPException(422, ...)` on miss, matching the `_VALID_CURRENCIES` pattern used by `get_aggregates`. Repo can then assume the value is valid (or keep the dict lookup but raise instead of silently skipping). **[Agent: python-backend]**
- [x] Document `period_start` UTC semantics — add a `description=` to the `period_start` field on the `AggregateItem` Pydantic model in `routers/transactions.py`. Phrasing: `"Bucket start in UTC. The bucket boundary is computed in the user's timezone (from user_settings.timezone) but serialized as a UTC ISO timestamp. For a Europe/Kyiv user, the January 2026 bucket appears as '2025-12-31T22:00:00Z' (= 2026-01-01T00:00 Kyiv)."` This makes the timezone behavior visible in OpenAPI/Swagger so frontend authors don't assume the bucket key is in their local zone. **[Agent: python-backend]**
- [x] Fix line-length warnings — in `services/api/src/grosh_api/routers/transactions.py`, two lines (~lines 183 and 199 per the most recent review) exceed the 88-char limit. Reflow without altering behavior. Run ruff over the file and confirm clean. **[Agent: python-backend]**
- [x] Add integration tests for `/transactions/aggregates` — new test module `services/api/tests/integration/test_aggregates.py` using the existing real-Postgres fixtures from `tests/integration/conftest.py`. Cover: (1) empty result when no transactions exist; (2) basic monthly bucketing with mixed UAH/USD/EUR amounts; (3) `currency=UAH` filter returns only UAH key in `currencies` dict; (4) `fields=delta` returns only `delta_cents` and `converted_pct` per currency (income/expense omitted via `response_model_exclude_none`); (5) transfers (`special_category='transfer'`) and zero-direction rows are excluded from sums; (6) `converted_pct < 100.0` when some rows have `NULL amount_eur_cents`; (7) timezone bucketing — insert a transaction at `Jan 31 21:30 UTC`, set `user_settings.timezone='Europe/Kyiv'`, confirm the row falls in the January bucket whose `period_start` is `2023-12-31T22:00:00Z` (= Jan 1 00:00 Kyiv); (8) invalid `bucket`, `currency`, `fields` → 422; (9) poisoned `user_settings.timezone='Not/AZone'` → 422 (not 500). **[Agent: python-backend]**
- [x] Add integration tests for half-open boundaries — new test module `services/api/tests/integration/test_time_range_boundaries.py`. Cover: (1) `GET /transactions?from=X&to=Y` excludes a row whose `time = Y` exactly; (2) `GET /transactions/aggregates?from=X&to=Y` does not double-count a row at `Y`; (3) `GET /rates?from=X&to=Y` excludes a row whose `valid_from = Y` exactly; (4) `GET /rates/at=T` returns rows with `valid_from <= T AND (valid_to IS NULL OR valid_to > T)` (SCD2 path remains inclusive on the lower bound — that's correct, this test pins the contract). These tests are the regression guard for Slice 15b. **[Agent: python-backend]**
- [x] Add integration test for the `unconverted_currency` filter — extend `tests/integration/test_aggregates.py` (or transactions list tests, wherever fits): seed a row with `amount_usd_cents = NULL`, call `GET /transactions?unconverted_currency=USD`, confirm the row appears; call `GET /transactions?unconverted_currency=GBP`, confirm 422 (regression for the silent-skip fix above). **[Agent: python-backend]**
- [x] Verify — `make test` (or the equivalent) runs all new integration tests green. Confirm coverage on `transaction_repo.get_aggregates`, `rate_repo._rate_filters`, and the router-layer validation increased. Open `/docs` and confirm the `period_start` field shows the new description. **[Agent: python-backend]**

---

## Slice 15d: Drop the `unconverted_currency` query param

**Design:** Slice 15 introduced `GET /transactions?unconverted_currency=X` as a server-side drill-down filter to complement the `converted_pct` quality metric on `/transactions/aggregates`. Slice 15c hardened it (silent-skip fix, integration tests). On reflection, the param is speculative API surface for a frontend that doesn't exist yet — and structurally it makes one endpoint operate in two unrelated modes ("list my transactions" vs. "show me data-quality holes"). At our scale (~50 tx/month, ~4% miss rate) the right tool is likely client-side highlighting or a dedicated `/transactions/unconverted` endpoint, but we don't yet know enough to choose.

**Decision:** delete the param now, defer the drill-down design until a real frontend access pattern emerges. `converted_pct` on `/transactions/aggregates` stays — it's the load-bearing metric. This supersedes the silent-skip fix and the three `test_unconverted_currency_*` tests added in Slice 15c — both were correct given the param existed, but the param itself is going away.

- [x] Remove `unconverted_currency` from `routers/transactions.py` — drop the `Query(...)` param, the `_VALID_CURRENCIES` validation block (the constant itself stays — `get_aggregates` still uses it), and the two pass-throughs to `count_transactions` / `list_transactions`. **[Agent: python-backend]**
- [x] Remove from `repositories/transaction_repo.py` — drop the `_UNCONVERTED_CURRENCY_COLUMNS` dict, the `unconverted_currency` parameter on `_build_transaction_conditions` / `count_transactions` / `list_transactions`, and the WHERE-clause branch (including the defensive `raise ValueError`). **[Agent: python-backend]**
- [x] Delete the three `test_unconverted_currency_*` tests in `tests/integration/test_aggregates.py` and update the module docstring to drop "and `unconverted_currency`". **[Agent: python-backend]**
- [x] Remove the `unconverted_currency` query entry from the "List transactions" request in `infra/grosh.postman_collection.json`. **[Agent: python-backend]**
- [x] Update `functional-spec.md` — §2.6 acceptance criterion (drop the `unconverted_currency` mention); §2.7 drill-down note (replace with "frontend can surface unconverted rows by filtering the list client-side; a dedicated endpoint may be added if access patterns warrant"). **[Agent: python-backend]**
- [x] Update `technical-considerations.md` — delete the dedicated "Conversion-quality drill-down" paragraph and prune the `/transactions` API contract row. **[Agent: python-backend]**
- [x] Add an `> **Update (2026-05-06):** ...` callout to `references/adr-drop-timescaledb.md` near the original drill-down mention noting that the per-row filter was deferred; do not edit the original prose (it documents the decision-time intent). **[Agent: python-backend]**
- [x] Verify — `uv run pytest services/api -q` passes (43 tests expected, was 46 minus the 3 deleted); `make lint` clean; `GET /openapi.json` no longer lists `unconverted_currency` on `/transactions`; `GET /transactions?unconverted_currency=USD` returns the same `total` as `GET /transactions` (FastAPI ignores unknown query params); `converted_pct` still appears in `/transactions/aggregates` responses. **[Agent: python-backend]**

---

## Slice 15e: Repeated-key + StrEnum for list-typed query params

**Design:** Slice 15 implemented `currency` and `fields` on `/transactions/aggregates` as comma-separated single-string params with hand-rolled validators (`_parse_csv_param`, `_parse_fields_param`). This is 2005-Java-style boilerplate that the type system should handle for free. Migrate to repeated-key + `StrEnum` per the new convention in `CLAUDE.md`: FastAPI parses repeated keys natively, validates membership via the enum, and emits a proper OpenAPI array schema with enum constraint that generated TypeScript SDKs can consume as `Currency[]`. Same enum-tightening applies to `direction` and `special_category` on `GET /transactions` — even as scalars, they carry enum constraints into OpenAPI. Strict canonical case (no `.upper()`/`.lower()` normalization): `?currency=uah` returns 422.

This is a behavior change for clients that send the comma-separated form. There is no production frontend yet, so the migration cost is limited to Postman, integration tests, and spec text.

- [x] Define enums — add `Currency` (`StrEnum` of `UAH`/`USD`/`EUR`) and `AggregateField` (`StrEnum` of `income`/`expense`/`delta`) at the top of `services/api/src/grosh_api/routers/transactions.py` (or in a new `services/api/src/grosh_api/enums.py` if you prefer). Promote `transaction_direction` (already a Postgres enum) and `special_category` to Python `StrEnum` siblings — `Direction` (`income`/`expense`/`zero`) and `SpecialCategory` (`transfer`). Bucket sizes likewise — `Bucket` (`day`/`week`/`month`/`quarter`/`year`) replaces the `_VALID_BUCKETS` frozenset. **[Agent: python-backend]**
- [x] Migrate `get_aggregates` to enum params — change `currency: str = Query("UAH,USD,EUR")` to `currency: list[Currency] = Query(default=[Currency.UAH, Currency.USD, Currency.EUR])`. Same for `fields: list[AggregateField] = Query(default=[AggregateField.income, AggregateField.expense, AggregateField.delta])`. `bucket: Bucket = Query(default=Bucket.month)`. Delete `_parse_csv_param`, `_parse_fields_param`, `_VALID_CURRENCIES`, `_VALID_FIELDS`, `_VALID_BUCKETS`. Update the response-building loop — `requested_currencies` and `requested_fields` are already `list[StrEnum]`; iteration logic is unchanged because StrEnum members are `str` subclasses. **[Agent: python-backend]**
- [x] Migrate `list_transactions` to enum params — change `direction: str | None = Query(None)` to `direction: Direction | None = Query(None)`, `special_category: str | None = Query(None)` to `special_category: SpecialCategory | None = Query(None)`. Repo signatures stay typed as `str | None` (StrEnum is `str`-compatible) so the repo doesn't need to import the router-layer enums. **[Agent: python-backend]**
- [x] Update Postman — split the "Aggregates" request's `currency` query entry from one comma-joined value into three separate disabled entries (`currency=UAH`, `currency=USD`, `currency=EUR`). Same for `fields` (three entries: `income`/`expense`/`delta`). Postman's "List transactions" request: no shape change, but the `direction`/`special_category` value examples already use canonical case. **[Agent: python-backend]**
- [x] Update integration tests — in `services/api/tests/integration/test_aggregates.py`, change every `?currency=UAH,USD,EUR` to `?currency=UAH&currency=USD&currency=EUR`, every `?fields=income,expense,delta` to `?fields=income&fields=expense&fields=delta`. Single-value calls (`?currency=UAH`) need no change. Add a new test asserting `?currency=uah` returns 422 (canonical-case enforcement regression guard). Add a new test asserting `?currency=UAH&currency=USD` returns both currencies in the response (positive repeated-key test). **[Agent: python-backend]**
- [x] Verify — `uv run pytest services/api -q` passes (45 expected: 43 today + 2 new tests). `make lint` clean. `curl -s http://localhost:8000/openapi.json | jq '.paths."/transactions/aggregates".get.parameters'` shows `currency` and `fields` as `type: array` with `items.enum`. `?currency=UAH&currency=USD` returns 2 currencies in response; `?currency=uah` returns 422; `?currency=UAH,USD` returns 422 (now treated as a single bad enum value, not a list — confirm the error message is helpful). **[Agent: python-backend]**

---

## Slice 15f: Shared `build_where` helper for optional-filter SQL assembly

**Design:** Three repos in `services/api` (`account_repo`, `transaction_repo`, `rate_repo`) hand-roll dynamic `WHERE` clause assembly with conditional predicates and manual `param_idx` arithmetic. Two competing styles are in use (a `param_idx` counter with `.append()` blocks, and a `(expr, value)` tuple list with enumeration), and the SCD2 lookup in `get_rates_at` patches the resulting string post-construction. Audit confirms no equivalent code in `consumer`, `ingestion`, `ml`, or `shared`.

Extract a single helper `build_where(filters: list[tuple[str, object | None]]) -> tuple[str, list[object]]` that takes `(expr_left, value)` pairs, skips `None` values, returns a condition string (no `WHERE` keyword) and a flat params list. Lives at `services/api/src/grosh_api/repositories/_sql.py` — API-internal, not promoted to `shared/` (no cross-service consumer). Refactor all three repos to use it. The SCD2 condition in `get_rates_at` becomes a regular filter entry (`("valid_to IS NULL OR valid_to >", at)`) — no string-replace surgery.

Why not pypika or SQLAlchemy Core: pypika doesn't speak asyncpg's `$N` placeholders natively and falls apart on Postgres-specific SQL (`FILTER`, `AT TIME ZONE`, `COALESCE` with typed literals) — strictly worse than today. SA Core would model the queries properly but is too heavy for three call sites; revisit if/when the pattern spreads beyond the API service.

- [x] Create `services/api/src/grosh_api/repositories/_sql.py` with `build_where(filters)` — accepts `list[tuple[str, object | None]]` where each pair is `(expr_left, value)`, e.g. `("time >=", from_time)` or `("valid_to IS NULL OR valid_to >", at)`. Skips entries where `value is None`. Returns `(condition_str, params)` — `condition_str` is `'a = $1 AND b >= $2'` (no leading `WHERE`), or empty string if no filters apply. Module-private (underscore prefix) — only repos import it. **[Agent: python-backend]**
- [x] Add unit tests `services/api/tests/unit/test_sql.py` — test the builder in isolation. Cover: empty list → `("", [])`; single filter → `("a = $1", [v])`; multiple filters → `("a = $1 AND b = $2", [v1, v2])`; all-None entries skipped → `("", [])`; mixed None and non-None preserves order; sentinel-style compound expr (`"valid_to IS NULL OR valid_to >"`) renders correctly with one trailing `$N`; non-None falsy values (`False`, `0`, `""`) preserved (only `None` is skipped). **[Agent: python-backend]**
- [x] Refactor `account_repo.list_by_user` — replace the `param_idx` block with a `build_where` call. Note `name` filter uses `ILIKE` and wraps the value in `%...%` — keep that wrap at the call site (`("name ILIKE", f"%{name}%" if name else None)`). Caller composes `f"... WHERE {where} ORDER BY created_at"`. **[Agent: python-backend]**
- [x] Refactor `transaction_repo._build_transaction_conditions` — replace the `param_idx` block with `build_where`. The `user_id = $1` predicate becomes the first filter entry (always non-None, so always present). Cursor pagination in `list_transactions` continues to compute `len(params) + 1` to extend the WHERE clause — no change there. The function continues to return `(condition_str, params)`; callers compose into the query template. **[Agent: python-backend]**
- [x] Refactor `rate_repo` — replace `_build_where` (the local helper) with imports of the shared `build_where`. In `get_rates_at`, model the SCD2 window as a regular filter entry (`("valid_to IS NULL OR valid_to >", at)`) — eliminates the `where_clause.replace(...)` surgery and the empty-where fallback. The `at` value will appear twice in `params` (once for `valid_from <=`, once for `valid_to >`); Postgres handles this cleanly. **[Agent: python-backend]**
- [x] Verify — `uv run pytest services/api -q` passes (53 expected: 45 today + ~8 new unit tests, give or take). `make lint` clean. Live API smoke checks: `GET /transactions?direction=expense&from=2026-04-01&to=2026-05-01` returns same row count as before refactor; `GET /accounts?source=monobank` works; `GET /rates?source=nbu` works; `GET /rates/at` (default = now) returns same number of active rates as before. Confirm `git diff --stat` shows net deletion across the three repo files. **[Agent: python-backend]**

---

## Slice 16: Transaction reprocessing — backup, delete, replay

**Design:** Implement the reprocessing job per `references/adr-transaction-reprocessing.md`. Reads existing transactions from DB, reconstructs `NormalizedTransaction` from stored columns (including `rate_source`), backs up to `reprocessing_backups`, deletes originals, publishes to `normalized_transactions` for fresh pipeline processing. Uses session-scoped `pg_advisory_lock` for mutual exclusion against itself; the normalization consumer routes events for locked users into a staging buffer table and drains them via LISTEN/NOTIFY on lock release. Verification via Kafka offset tracking.

The slice also lands the **`metadata` wrapping convention** — the prerequisite that makes reprocess decoupled from pipeline-layer internals. Source-side keys live under `metadata.source`, layer-side keys under `metadata.layer.<namespace>`. Reprocess drops `metadata.layer` and preserves `metadata.source`; one rule, no per-layer enumeration. The first three tasks below establish this convention (refactor layers, refactor normalizer, SQL-migrate existing rows) before the reprocess job tasks consume it.

> **Local DB note (2026-05-07):** migration 0004's `uuidv7()` defaults on `transfer_match_anomalies.id` and `reprocessing_backups.id` were applied to the existing dev DB via in-place `ALTER TABLE ... SET DEFAULT uuidv7()` (a metadata-only change preserving all existing rows, which include real transactions data needed for Slice 16's reprocessability test). Pre-surgery `pg_dump` archived at `uncommitted/grosh-backup-20260507-005308.dump`. On a fresh DB the migration applies the new defaults directly — no surgery needed.

- [ ] Wrap pipeline-layer metadata under `metadata.layer.<namespace>` — refactor every place a pipeline layer writes to the row's `metadata` JSONB so its output lands under its own sub-namespace, not flat. Currency conversion (today writes flat `rate_uah`/`rate_usd`/`rate_eur` via `_merge_metadata` in `services/consumer/src/grosh_consumer/services/pipeline.py`) becomes `metadata.layer.rate = {uah, usd, eur}`. Transfer detection v2 (per `adr-transfer-detection-v2.md`) writes `metadata.layer.transfer`. The orchestrator's merge function changes from flat `dict.update()` to a structured merge into `metadata.layer.<namespace>`. Each layer becomes responsible for naming its own sub-key. Update unit + integration tests that assert against flat metadata keys. This unlocks the wrapping convention that the reprocess job depends on. **[Agent: python-backend]**
- [ ] Wrap source metadata under `metadata.source` — refactor the normalizer (today writes flat keys like `counter_name`, `counter_edrpou`, `comment`, `receipt_id` into `NormalizedTransaction.metadata`) so it nests them under `metadata.source = {...}`. Audit each source's normalization adapter (`services/consumer/src/grosh_consumer/sources/{source}/normalize.py` or equivalent). Update tests that assert flat source keys. After this lands, all newly-ingested rows have the wrapped shape; existing rows are migrated by the SQL surgery task below. **[Agent: python-backend]**
- [ ] One-shot SQL migration: wrap existing flat `metadata` into `metadata.source` — write an Alembic migration (or a one-off `services/api/migrations/scripts/`-style script) that takes every existing `transactions` row and transforms its flat `metadata` JSONB into `{"source": {... non-layer keys ...}, "layer": {... existing layer keys ...}}`. Identify which keys are source-side vs layer-side via a one-time enumeration: layer keys present in existing rows today are `rate_uah`, `rate_usd`, `rate_eur` (currency conversion — the only pipeline layer that has been writing metadata so far). Everything else is source. Future layers will add their own sub-namespaces under `metadata.layer.<name>` (Slice 17's transfer detection v2 will write `metadata.layer.transfer`); the wrapping refactor that lands in this slice is what makes those additions safe. The migration must be idempotent (re-running on already-wrapped metadata is a no-op). Run on the dev DB after the wrapping refactor lands, before the reprocess job runs. **Why we can't replay this through the pipeline:** reprocess preserves source metadata as-is and regenerates only layer metadata, but it doesn't re-run normalization (per the boundary in `adr-consumer-pipeline-architecture.md`). So source-side wrapping has to be done by SQL surgery; layer-side wrapping happens automatically when the reprocess sweep regenerates layer outputs under the new convention. **[Agent: postgres-database]**
- [ ] Make `transactions.hold` nullable — preparation for non-Monobank sources (PUMB, Revolut) that don't expose a per-row `hold` flag. The Mono webhook always populates it; PUMB has no equivalent (settled vs pending is encoded structurally in the response array, not as a per-row column); Revolut Business has a richer `state` enum we'd ignore. Better to honestly write `NULL` from non-Mono normalizers than to fake `false`. **Update migration 0004 in-place** (it's an unmerged-by-the-time-Slice-16-runs migration on this branch; per project rules we modify rather than create a new one): change `hold BOOLEAN NOT NULL DEFAULT false` to `hold BOOLEAN` (no NOT NULL, no default). **Apply on the dev DB via in-place surgery** — do NOT run a new Alembic migration for this. The dev DB has real Slice 14b transaction data we want to preserve, so run `ALTER TABLE transactions ALTER COLUMN hold DROP NOT NULL; ALTER TABLE transactions ALTER COLUMN hold DROP DEFAULT;` directly via `psql` (mirrors the pattern used for the 2026-05-07 `uuidv7()` surgery noted at the top of this slice). On a fresh DB the updated migration applies the new shape directly — no surgery needed. Update the `transactions` row in `technical-considerations.md` §2.2 (already says `hold BOOLEAN` without NOT NULL — confirm and tighten the wording if needed). Update `NormalizedTransaction.hold` type from `bool` to `bool | None`. Update Mono normalizer to keep populating it (no behavior change). No tests should break — `hold` is "stored as-is, not used for filtering or branching" per tech spec line 696. **[Agent: postgres-database]**
- [ ] Create the `staging_normalized_transactions` table — new Alembic migration (next free number — verify at deploy time, currently 0009): `CREATE TABLE staging_normalized_transactions (id UUID PRIMARY KEY DEFAULT uuidv7(), user_id UUID NOT NULL, payload JSONB NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now())`. Add `CREATE INDEX idx_staging_user_created ON staging_normalized_transactions (user_id, created_at)` for the drain task's per-user ordered fetch and the periodic sweep's `WHERE user_id NOT IN (...)` scan. **No FK on `user_id`** — per `adr-transaction-reprocessing.md` "this is an operational queue, not a relational entity" (rows live for seconds-to-minutes during a reprocess and are explicitly drained, never long-lived enough to need referential integrity to `users`). No RLS — internal-only table written by the consumer (which bypasses RLS). The migration must also `GRANT INSERT, DELETE ON staging_normalized_transactions TO grosh_consumer` (matching the pattern in migration 0007 for `reprocessing_locks` and `reprocessing_backups`). The migration's `down()` reverses each step (DROP INDEX, DROP TABLE, REVOKE). **[Agent: postgres-database]**
- [ ] Add staging-buffer routing to normalization consumer — the normalization consumer **does NOT acquire the advisory lock**; only the reprocess job does. The consumer's job is to read the routing signal (`reprocessing_locks` row) and decide whether to publish to Kafka or stage. In a single DB transaction wrapping the publish/stage decision: check `EXISTS (SELECT 1 FROM reprocessing_locks WHERE user_id = $1)`; if present, INSERT the `NormalizedTransaction` payload into `staging_normalized_transactions` (via `staging_repo.insert_staged(user_id, payload)`); otherwise publish to `normalized_transactions` as today. The check and the publish/stage decision MUST commit in the same transaction — splitting them creates a race where reprocess could acquire the lock between the check and the write, leaking events past the buffer. Implement the staging persistence in a new `services/consumer/src/grosh_consumer/repositories/staging_repo.py` with methods `insert_staged(user_id, payload)`, `select_staged_for_user(user_id) -> list[Row]` (ORDER BY `created_at`), `delete_staged(id)`. See `adr-transaction-reprocessing.md` "Concurrency" for the full design. **[Agent: python-backend]**
- [ ] Add staging drain task to normalization consumer — implement in a new `services/consumer/src/grosh_consumer/consumers/staging_drain.py`, started as a background asyncio task inside the normalization consumer's lifespan (not a separate process; same Kafka consumer group, same DB pool). Long-lived asyncpg listener connection on the `reprocess_complete` channel: on receiving a NOTIFY, parse the user_id payload and call `drain_for_user(user_id)` which uses `staging_repo.select_staged_for_user(user_id)` to fetch rows ORDER BY `created_at`, publishes each to `normalized_transactions`, then `staging_repo.delete_staged(id)` after each successful publish (publish-then-delete ordering for at-least-once — duplicate publishes on crash are caught by the pipeline consumer's `ON CONFLICT (id) DO NOTHING`). Also implement a periodic 60s sweep that drains `WHERE user_id NOT IN (SELECT user_id FROM reprocessing_locks)` as the **non-negotiable safety net** for missed notifications (NOTIFY is best-effort; a network blip or listener reconnect can drop the signal). **[Agent: python-backend]**
- [ ] Create the NormalizedTransaction reconstruction module — `services/consumer/src/grosh_consumer/jobs/reconstruct.py` exposing `reconstruct(row: TransactionRow) -> NormalizedTransaction`. Maps stored columns onto the model per the ADR's "Preserved vs. re-derived fields" section. `direction` is preserved as-is (immutable post-Slice 14b). `special_category` is **not** on `NormalizedTransaction` — it is recomputed by the pipeline's transfer detection layer on replay. Metadata reconstruction: `metadata_for_replay = {"source": (stored_row.metadata or {}).get("source", {})}` — drop `metadata.layer` entirely; layers regenerate. Reference `services/consumer/src/grosh_consumer/models/normalized.py` for the authoritative field list. The reprocess job in the next task imports and calls this function row-by-row. **[Agent: python-backend]**
- [ ] Create reprocessing job — `services/consumer/src/grosh_consumer/jobs/run_reprocess.py`: accepts optional `user_ids` param (default: all users). Per user, holding a session-scoped `pg_advisory_lock(hashtext('reprocess:' || user_id::text))` for the entire job lifetime (acquire on a dedicated session connection at job start, release at job end via `pg_advisory_unlock(hashtext('reprocess:' || user_id::text))`): (1) clean up stale `reprocessing_locks` rows (>30 min — the prior session's advisory lock has already auto-released on session end, so the row is the only thing left), (2) atomic transaction: INSERT into `reprocessing_locks` + acquire advisory lock on the same session, (3) INSERT snapshot into `reprocessing_backups`, (4) SELECT all transactions for user ordered by `(time, id)`, (5) reconstruct `NormalizedTransaction` for each row by calling `reconstruct(row)` from `jobs/reconstruct.py`, (6) DELETE all transactions for user (CASCADE clears anomalies, self-FK SET NULL on `related_transaction_id` is a no-op since all rows are deleted together), (7) publish replay events to `normalized_transactions` directly (bypassing staging — the reprocess job is the lock holder, not a normalization-consumer event), (8) wait for pipeline consumer to catch up (poll committed offsets), (9) verify all IDs from snapshot exist in DB, (10) atomic transaction: DELETE from `reprocessing_locks` + `pg_advisory_unlock` + `NOTIFY reprocess_complete, $user_id` (NOTIFY only fires on commit — Postgres semantics — so the staged-event drain is signalled exactly when the lock-row visibility releases), (11) on verification failure: restore from backup, clear lock, alert. **[Agent: python-backend]**
- [ ] Create reprocess endpoint — add `POST /reprocess` to consumer service's HTTP server (or create a minimal HTTP server if consumer doesn't have one). Authenticated via JWT, acts on current user. Returns 409 if lock already exists, 429 if cooldown not elapsed (1h). **[Agent: python-backend]**
- [ ] Create K8s job template — `infra/k8s/reprocess-job-template.yaml`: uses consumer service image, entrypoint `python -m grosh_consumer.jobs.run_reprocess`, accepts `USER_IDS_JSON` env var (JSON array of UUID strings, e.g. `'["uuid1","uuid2"]'`; unset means all users). The job entrypoint parses with `json.loads` and validates each element as `UUID` — invalid JSON or non-UUID elements raise loudly at startup. **[Agent: k8s-infra]**
- [ ] Verify — insert test transactions for a user. Trigger `POST /reprocess`. Confirm: (1) `reprocessing_locks` row created during processing, (2) transactions temporarily disappear, (3) all transactions reappear after pipeline consumer processes them, (4) `reprocessing_locks` row deleted, (5) `reprocessing_backups` row exists with full snapshot, (6) `metadata.layer.*` is regenerated on replayed rows (not the v1-era flat keys), (7) `metadata.source` is preserved byte-identical to the pre-reprocess snapshot. Test concurrent webhook during reprocess: send a webhook event while reprocessing is in progress — confirm it lands in `staging_normalized_transactions` and is drained on lock release (verify by polling staging table during reprocess). Test failure recovery: kill the reprocess job mid-flight, confirm backup exists, advisory lock is released (session ended), and the periodic sweep eventually drains any staged events. Test the K8s template by setting `USER_IDS_JSON='["<one-real-uuid>"]'` and confirming only that user is reprocessed; also confirm invalid JSON or a non-UUID element fails loudly at job startup. Reprocessability invariant check: capture a transaction's full DB row before reprocess, run reprocess, confirm the post-replay row matches the snapshot on all preserved fields (including `metadata.source`) and differs only on documented re-derived fields (`special_category`, `amount_uah_cents`/`amount_usd_cents`/`amount_eur_cents`, `related_transaction_id`, `metadata.layer.*`). **[Agent: python-backend]**

---

## Slice 17: Rebuild Monobank transfer detection per the v2 ADR

**Design:** Replace the existing single-file `MonobankTransferDetection` strategy and its tier-specific repo queries with the module layout specified in `references/adr-transfer-detection-v2.md` and the "Transfer detection (Monobank strategy)" subsection of `technical-considerations.md`. The new strategy runs one universal candidate fetch, applies a hard IBAN consistency filter, classifies per-pair evidence (`bilateral`/`unilateral`/`none`), and decides via a count-and-decide branch with the bucket-locked principle. It writes `metadata.layer.transfer.{row, pair?}` on every MCC 4829 row through a new `metadata_block` field on `TransferResult`, which the orchestrator merges via the structured per-layer merge function landed in Slice 16.

**Why a full rebuild instead of an in-place refactor:**
1. The module shape inverts. v1's three near-identical tier methods + interleaved anomaly literals + SQL glue have no incremental path to the 8-module split (`detector` orchestrator + pure-function helpers `flags`, `iban_classifier`, `decision`, `metadata`, `anomalies` + repo + descriptions). Almost nothing transplants.
2. The SQL fully changes. `find_unclaimed_partner_tier_a/b/c` collapse into one `find_universal_candidates` with a two-clause amount predicate and a different result-handling contract.
3. The `metadata.layer.transfer` block is a new write surface; intermediate states between the two designs would be Frankenstein code with no clean test boundary.

**What is preserved (NOT rewritten):** `sources/monobank/descriptions.py` (extended with one helper); `repositories/anomaly_repo.py` (used as-is); `repositories/account_property_repo.py` (used as-is); `services/transfer_detection.py` Protocol shape (signature unchanged — gains a new field on `TransferResult`); the strategy's registration in the pipeline orchestrator (key `"monobank"` in `TRANSFER_STRATEGIES`).

**Prerequisites:** Slice 16 must be merged AND its reprocess sweep must have run successfully across all users. v2 depends on the `metadata.source` / `metadata.layer.<name>` wrapping convention and the structured `_merge_metadata` function — see ADR "Prerequisite: metadata wrapping convention". Specifically, the Slice 16 tasks Slice 17 depends on are:

1. *Wrap pipeline-layer metadata under `metadata.layer.<namespace>`* — supplies the structured `_merge_metadata` function this slice's orchestrator-wire task plugs into.
2. *Wrap source metadata under `metadata.source`* — required so the wrapping convention is consistent across both source and layer namespaces.
3. *One-shot SQL migration: wrap existing flat `metadata` into `metadata.source`* — required so existing rows have the wrapped shape before this slice's reprocess sweep runs (otherwise reprocess can't preserve `metadata.source` correctly).
4. *Create reprocessing job* + *Verify* (the final two Slice 16 tasks) — must have been executed at least once across all users so that no `transfer_match_anomalies` row still references the v1-only enum values. The schema migration below has a pre-flight assert that fails loudly if this isn't true.

The advisory-lock + staging-routing tasks in Slice 16 are not strict prerequisites for Slice 17's code to function, but they are prerequisites for Slice 16's reprocess sweep to be safe under concurrent webhook load — so they're transitively required.

**Deletion list (committed at the start of the slice, before any new code):**
- `services/consumer/src/grosh_consumer/sources/monobank/transfer.py`
- `services/consumer/src/grosh_consumer/repositories/transfer_repo.py`
- `services/consumer/tests/integration/test_transfer_tier_a_integration.py`
- `services/consumer/tests/integration/test_transfer_tier_b_integration.py`
- `services/consumer/tests/integration/test_transfer_tier_c_integration.py`
- `services/consumer/tests/unit/test_transfer_service_tier_a.py`
- `services/consumer/tests/unit/test_transfer_service_tier_b.py`
- `services/consumer/tests/unit/test_transfer_service_tier_c.py`
- `services/consumer/tests/unit/test_transfer_service_anomalies.py`
- `services/consumer/tests/unit/test_transfer_service_claim_locks.py`
- `services/consumer/tests/unit/test_transfer_service_idempotency.py`
- `services/consumer/tests/unit/test_transfer_service_multi_hop.py`
- `services/consumer/tests/unit/test_transfer_result.py`
- `services/consumer/tests/unit/transfer_fixtures.py`

`test_service_end_to_end.py`, `test_transfer_pipeline_integration.py`, `test_transfer_anomaly_integration.py`, `test_description_guard.py`, and `conftest.py` are reviewed and updated, not deleted (most still apply; only v1-specific assertions are rewritten — see verification step).

The slice deliberately starts with a single deletion commit so reviewers can `git diff` the new code against an empty baseline rather than against a moving v1 file. CI will be red after the deletion commit and stay red until the strategy + repo + registry rewire land — that is acceptable on the slice branch (not on `main`).

- [ ] Schema migration — drop the three v1-only `transfer_anomaly_reason` enum values and their three partial indexes. Single Alembic migration (next free number — verify at deploy time): (a) `DROP INDEX idx_transactions_transfer_tier_a, idx_transactions_transfer_tier_b, idx_transactions_transfer_tier_c`; (b) `CREATE INDEX idx_transactions_transfer_universal ON transactions (user_id, direction, time DESC) WHERE mcc = '4829' AND related_transaction_id IS NULL`; (c) add `'ambiguous_pair_match'` value to `transfer_anomaly_reason`; (d) drop values `'ambiguous_iban_match'`, `'ambiguous_reverse_iban'`, `'ambiguous_amount_match'` via the recreate-and-swap recipe (Postgres has no `ALTER TYPE … DROP VALUE`). The migration's `down()` reverses each step. Pre-flight assert in `up()`: `SELECT count(*) FROM transfer_match_anomalies WHERE reason_code IN ('ambiguous_iban_match', 'ambiguous_reverse_iban', 'ambiguous_amount_match') = 0` — raise loudly if non-zero, since dropping referenced enum values will fail. The Slice 16 reprocess sweep is what zeros this set. **Final enum values after this migration (exactly 5, in declaration order for the recreated type):** `unpaired_from_description`, `unpaired_to_description`, `ambiguous_pair_match`, `description_account_mismatch`, `description_consistency_mismatch`. The recreate-and-swap recipe creates `transfer_anomaly_reason_new` with these 5 values, runs `ALTER TABLE transfer_match_anomalies ALTER COLUMN reason_code TYPE transfer_anomaly_reason_new USING reason_code::text::transfer_anomaly_reason_new`, drops the old type, then renames new → old. **[Agent: postgres-database]**
- [ ] Delete v1 strategy, repo, and v1-specific tests in a single commit — exactly the files listed in the slice's "Deletion list" above. Do not stage replacements in the same commit; the goal is a clean baseline for `git diff`. CI will be red after this commit. **[Agent: python-backend]**
- [ ] **Scaffold the full unit-test suite BEFORE any module is implemented** — write every test case from `references/consumer-transfer-detection-test-suite.md` §§2–8 as runnable pytest functions. For each test file targeting a yet-to-exist module (`test_transfer_flags.py`, `test_transfer_iban_classifier.py`, `test_transfer_metadata.py`, `test_transfer_anomalies.py`, `test_transfer_decision.py`, `test_transfer_result.py`), put a module-level `pytestmark = pytest.mark.skip(reason="awaiting <module>.py implementation")` at the top so the suite collects but doesn't fail; remove the marker as part of each module's own task (see the per-module tasks below). For `test_description_guard.py`, extend in-place — the existing module already exists and the new `is_multi_hop_description` cases run live alongside the existing ones (no skip marker; the extension task below picks up the failing-then-green cycle). Also write the `tests/unit/conftest.py` extensions per test-suite doc §1.2: `make_normalized_tx`, `make_row_flags`, `make_candidate`, `make_account_props` factories, `iban_lookup_returning` / `iban_lookup_from_dict` stubs, and the test constants block (`T`, `T_PLUS_2S`, `WINDOW_SECONDS`, account fixtures `UAH_FOP`/`USD_FOP`/`UAH_BLACK`/etc.). The conftest factories MUST NOT import the v2 modules (they'd fail collection) — define `RowFlags` / `AccountProps` / `CandidateRow` as the test fixtures' own dataclass shadows that the implementation will replace by importing from production paths in each per-module task. After this task lands: `uv run pytest services/consumer/tests/unit -q` collects every v2 test, runs the live `test_description_guard.py` extension cases (which fail until the next task lands), and skips everything else with the documented reason. This is the test-first contract the rest of the slice consumes. **[Agent: python-backend]**
- [ ] **Scaffold the integration test suite alongside the unit suite, also BEFORE any module is implemented** — write every test case from `references/consumer-transfer-detection-test-suite.md` §§9–18 as runnable pytest functions. New files (each with a module-level `pytestmark = pytest.mark.skip(reason="awaiting <module> implementation")` removed by the per-module task identified):
  - `test_transfer_universal_fetch_integration.py` — gated by the `transfer_repo.py` task.
  - `test_transfer_consistency_filter_integration.py` — gated by the `detector.py` task.
  - `test_transfer_decision_integration.py` — gated by the `detector.py` task.
  - `test_transfer_metadata_block_integration.py` — gated by the `transfer_repo.py` task.
  - `test_transfer_directional_transitive_integration.py` — gated by the `detector.py` task.
  - `test_transfer_external_short_circuit_integration.py` — gated by the `detector.py` task.
  - `test_transfer_amount_asymmetry_integration.py` — gated by the `detector.py` task.

  Updated files (existing files restructured against v2 enum + algorithm steps, with skip markers gated by the orchestrator-wire task):
  - `test_transfer_pipeline_integration.py`, `test_transfer_anomaly_integration.py` — replace v1 ambiguity-code assertions with the v2 enum, replace tier-specific assertions with algorithm-pass assertions, keep auto-resolve cases.
  - `test_service_end_to_end.py` — v1 anomaly codes replaced; otherwise unchanged.

  Also extend `tests/integration/conftest.py` per test-suite doc §1.3: rewire the `transfer_repo` and `transfer_service` fixtures to instantiate the rebuilt repo + new `MonobankTransferDetection` (gate the imports behind `try/except ImportError` so the conftest still loads when the modules don't exist yet — skipped tests don't trigger the import), and add the `get_transfer_metadata(conn, tx_id) -> dict | None` helper with the safe-chained `.get(...)` implementation. After this task lands: `uv run pytest services/consumer/tests/integration -q` collects every v2 integration test and skips them all with the documented reason; the existing non-transfer integration tests continue to run live. **[Agent: python-backend]**
- [ ] Extend `descriptions.py` with `is_multi_hop_description(description: str | None) -> bool` — returns `True` when the description is in the `для переказу на` family (subset of `description_matched`). All other `descriptions.py` functions stay byte-identical. **Test gate:** `uv run pytest services/consumer/tests/unit/test_description_guard.py -q` is fully green before this task is marked done — including the new `is_multi_hop_description` cases scaffolded in the previous task and the pre-existing `parse_description` / `is_transfer_description` / `validate_pair_descriptions` cases. **[Agent: python-backend]**
- [ ] Add `metadata_block: dict | None = None` field to `TransferResult` in `services/transfer_detection.py` — default `None`. Update the dataclass docstring to specify the contract: `{"row": {...}}` on unpaired MCC 4829 rows, `{"row": {...}, "pair": {...}}` on claimed pairs, `None` for non-MCC-4829 rows or for sources whose strategy doesn't write a block. The `TransferDetectionStrategy` Protocol signature is unchanged. **Test gate:** remove the `pytestmark.skip` from `tests/unit/test_transfer_result.py` and confirm `uv run pytest services/consumer/tests/unit/test_transfer_result.py -q` is fully green (cases per test-suite §8: default shape, claim shape with `metadata_block`, anomaly-only shape, skip-on-non-MCC-4829, frozen-mutation, structural equality). **[Agent: python-backend]**
- [ ] Create `sources/monobank/transfer/__init__.py` and `sources/monobank/transfer/flags.py` — `RowFlags` frozen dataclass with fields `description_matched: bool`, `multi_hop_description: bool`, `cp_iban_status: Literal["null", "transitive", "external", "honest"]`. Pure function `compute_row_flags(tx: NormalizedTransaction, account_iban_lookup: Callable[[str], UUID | None]) -> RowFlags`. The directional transitive rule (`direction == "expense" AND multi_hop_description → cp_iban_status = "transitive"`) is one explicit branch in this function. `account_iban_lookup` returns the user's account UUID owning a given IBAN, or `None` for external IBANs — `external` vs `honest` is decided by its result. No DB access inside `flags.py` itself; the lookup is injected. **Test gate:** remove the `pytestmark.skip` from `tests/unit/test_transfer_flags.py`, also update `tests/unit/conftest.py` to import `RowFlags` from production (`from grosh_consumer.sources.monobank.transfer.flags import RowFlags`) replacing the test-local shadow, and confirm `uv run pytest services/consumer/tests/unit/test_transfer_flags.py -q` is fully green (all cases from test-suite §3, including the directional-transitive matrix and the realistic-composition cases 77–79). **[Agent: python-backend]**
- [ ] Create `sources/monobank/transfer/iban_classifier.py` — two pure functions, no DB, no IO. `is_consistent(incoming_flags: RowFlags, candidate_flags: RowFlags, incoming_account_iban: str | None, candidate_account_iban: str | None, incoming_cp_iban: str | None, candidate_cp_iban: str | None) -> bool` implements the hard consistency filter per ADR §4 (`null`/`transitive` impose no constraint; `honest`/`external` cp_iban must resolve to the other side's account). `classify_pair_evidence(...) -> Literal["bilateral", "unilateral", "none"]` per ADR §4 evidence classification rules. **Test gate:** remove the `pytestmark.skip` from `tests/unit/test_transfer_iban_classifier.py` and confirm `uv run pytest services/consumer/tests/unit/test_transfer_iban_classifier.py -q` is fully green — all cases from test-suite §4 including the full 4×4 `cp_iban_status × cp_iban_status` matrix (with pointing-correctly vs pointing-wrongly variations on `honest` cells) and the three evidence-level outcomes for `classify_pair_evidence`. **[Agent: python-backend]**
- [ ] Create `sources/monobank/transfer/metadata.py` — `build_row_block(flags: RowFlags) -> dict` returns `{"description_matched": bool, "multi_hop_description": bool, "cp_iban_status": str}`. `build_pair_block(iban_evidence: str, description_decisive: bool) -> dict` returns `{"iban_evidence": str, "description_decisive": bool}`. Centralizing the JSON-shape builders here means changes to `metadata.layer.transfer.{row, pair}` touch one file. **Test gate:** remove the `pytestmark.skip` from `tests/unit/test_transfer_metadata.py` and confirm `uv run pytest services/consumer/tests/unit/test_transfer_metadata.py -q` is fully green — snapshot comparison cases against the ADR §2 schema, the "exactly N keys" guards, and the JSON round-trip identity cases (test-suite §5). **[Agent: python-backend]**
- [ ] Create `sources/monobank/transfer/anomalies.py` — one builder per `AnomalyRecord` variant: `unpaired_from_description(tx, ...)`, `unpaired_to_description(tx, ...)`, `description_consistency_mismatch(tx, partner_id, income_desc, expense_desc, income_acc_props, expense_acc_props)`, `description_account_mismatch(tx, candidate_ids, incoming_desc, incoming_acc_props)`, `ambiguous_pair_match(tx, candidate_ids, bucket_evidence_level, reason)`. Each builder formats `reason_detail` from its inputs and returns a typed `AnomalyRecord`. The detector never constructs an `AnomalyRecord` inline. **Test gate:** remove the `pytestmark.skip` from `tests/unit/test_transfer_anomalies.py` and confirm `uv run pytest services/consumer/tests/unit/test_transfer_anomalies.py -q` is fully green — one passing case per builder asserting `reason_code`, `candidate_ids`, and that `reason_detail` mentions the inputs (test-suite §6). **[Agent: python-backend]**
- [ ] Create `sources/monobank/transfer/decision.py` — `Claim`, `Anomaly`, `Skip` frozen dataclasses (a discriminated union over `Decision`). `decide(candidates: list[CandidateRow], incoming_flags: RowFlags, incoming_acc_props: AccountProps, account_props_by_id: dict[UUID, AccountProps]) -> Decision` implements the full count-and-decide branch (ADR §1 step 6). Owns the bucket-locked principle as a single private `_select_bucket(candidates_by_evidence: dict[str, list]) -> tuple[str, list]` helper that returns the highest non-empty bucket. Calls `validate_pair_descriptions` (from `descriptions.py`) for both the count==1 canary path and the count>1 hard-filter path. Pure function — no DB, no `conn`. **Test gate:** remove the `pytestmark.skip` from `tests/unit/test_transfer_decision.py` and confirm `uv run pytest services/consumer/tests/unit/test_transfer_decision.py -q` is fully green — every case from test-suite §7 (count==0 with/without prefix; count==1 with each evidence level + canary on description mismatch + the new 129b case for `none` evidence + bad description; the full count>1 bucket-locked case set including the 3-bucket case 133b that pins the priority order; `Decision` discriminated-union shape cases). **[Agent: python-backend]**
- [ ] Create `repositories/transfer_repo.py` (the rebuilt repo) — three methods: `exists(conn, tx_id) -> bool`, `find_universal_candidates(conn, *, user_id, opposite_direction, incoming_account_id, incoming_amount_cents, incoming_op_amount_cents, time, window_seconds) -> list[CandidateRow]` (issues the `SELECT … FOR UPDATE SKIP LOCKED` per ADR §3 with the two-clause amount predicate), `claim_pair(conn, *, existing_id, new_id, pair_metadata_block: dict) -> None` (UPDATE sets `related_transaction_id`, `special_category = 'transfer'`, and merges `pair_metadata_block` under `metadata.layer.transfer.pair` on the partner row using `jsonb_set(COALESCE(metadata, '{}'::jsonb), …)`). SQL is triple-quoted, one item per line per project SQL style. `CandidateRow` is a frozen dataclass with the columns the decision needs (`id`, `account_id`, `direction`, `cp_iban`, `description`, `amount_cents`, `operation_amount_cents`, `time`). **Test gate (integration, not unit — the repo touches Postgres):** the integration test files exercising the repo (`test_transfer_universal_fetch_integration.py` and the `claim_pair` cases in `test_transfer_metadata_block_integration.py`) are written and skipped together with the unit suite in the scaffolding task; remove their `pytestmark.skip` once the repo is implemented and confirm `uv run pytest services/consumer/tests/integration/test_transfer_universal_fetch_integration.py services/consumer/tests/integration/test_transfer_metadata_block_integration.py -q` is fully green — including the §12.2 NULL-metadata defensive cases (197b/c/d) that pin the `COALESCE` requirement. **[Agent: python-backend]**
- [ ] Create `sources/monobank/transfer/detector.py` — the new `MonobankTransferDetection` class implementing `TransferDetectionStrategy.detect_and_pair(conn, tx) -> TransferResult`. The body reads top-down as the 7-step algorithm pass: (1) MCC + idempotency gate, (2) compute `RowFlags` via `flags.compute_row_flags`, (3) external short-circuit (skip fetch, build `metadata_block` with `row` only; if `flags.description_matched` is `False` and `is_transfer_description(tx.description)` from `descriptions.py` is `True`, emit `unpaired_from_description` (income side) or `unpaired_to_description` (expense side) — the prefix-but-unknown vocabulary-drift signal; if both are `False` or `description_matched` is `True`, emit no anomaly), (4) `transfer_repo.find_universal_candidates`, (5) drop candidates failing `iban_classifier.is_consistent`, (6) classify each survivor with `iban_classifier.classify_pair_evidence`, (7) call `decision.decide` and translate its `Decision` into a `TransferResult` (Claim → call `transfer_repo.claim_pair` + `anomaly_repo.delete_unpaired_anomalies_for_transactions([incoming.id, partner.id])` + return `TransferResult` with `special_category`, `related_transaction_id`, `metadata_block` containing both `row` and `pair`; Anomaly → return `TransferResult` with `metadata_block` containing only `row` plus the anomaly in `anomalies`; Skip → return `TransferResult` with `metadata_block` containing only `row`). Constructor takes the three repos via DI (`transfer_repo`, `account_property_repo`, `anomaly_repo`). No SQL, no description-map literals, no anomaly literals — all delegated. **Test gate (integration — the detector composes the repos):** the algorithm-step integration files (`test_transfer_consistency_filter_integration.py`, `test_transfer_decision_integration.py`, `test_transfer_directional_transitive_integration.py`, `test_transfer_external_short_circuit_integration.py`, `test_transfer_amount_asymmetry_integration.py`) all run against the new detector. After the detector is implemented, remove their `pytestmark.skip` and confirm `uv run pytest services/consumer/tests/integration -q -k transfer` is fully green (excluding the pipeline-orchestrator file `test_transfer_pipeline_integration.py`, which is gated by the next two tasks). **[Agent: python-backend]**
- [ ] Wire the orchestrator's metadata merge — update `services/pipeline.py` so `TransferResult.metadata_block`, when not `None`, is merged under `metadata.layer.transfer` on the row being persisted (using the structured per-layer merge from Slice 16). When `metadata_block` is `None`, write nothing under `metadata.layer.transfer` so the invariant `metadata.layer.transfer exists ⇔ mcc == '4829'` holds. The strategy registry dispatch and source-name lookup are unchanged — only the result handling extends. The orchestrator never inspects the contents of `metadata_block`; it's an opaque payload owned by the strategy. **Test gate:** remove the `pytestmark.skip` from `test_transfer_pipeline_integration.py` and confirm `uv run pytest services/consumer/tests/integration/test_transfer_pipeline_integration.py -q` is fully green — full-pipeline cases from test-suite §18 covering the metadata write on both legs (case 225), non-claim paths (cases 226–227), idempotency (cases 228–229), reprocess replay (case 229b), and source dispatch (cases 230–232). **[Agent: python-backend]**
- [ ] Wire the partner-row metadata write — already exercised by the integration tests in the previous tasks (`test_transfer_metadata_block_integration.py` cases 192–197d covers the `pair` block identity on both legs and the `jsonb_set` defensive cases for NULL / empty / source-only metadata; `test_transfer_pipeline_integration.py` case 225 covers the end-to-end orchestrator + claim_pair interplay). This task confirms the wiring works without writing new tests: re-run `uv run pytest services/consumer/tests/integration -q -k transfer` and confirm all transfer-related integration tests pass; spot-check one paired transfer in dev DB by querying both rows and confirming `metadata->'layer'->'transfer'->'pair'` is JSON-equal byte-for-byte. **[Agent: python-backend]**
- [ ] Verify — `uv run pytest services/consumer -q` passes (full suite green, no remaining `pytestmark.skip` markers from the v2 scaffolding tasks; expected count: existing non-transfer tests + 6 new unit test files + extended `test_description_guard.py` + 7 new integration test files + 3 updated integration test files). `make lint` clean. Run a manual end-to-end check by replaying a representative slice of dev-DB transactions through `POST /reprocess` for the dev user; query `SELECT mcc, metadata->'layer'->'transfer' IS NULL AS no_block FROM transactions GROUP BY 1, 2` and confirm the only `no_block = false` rows are MCC 4829. Spot-check 3 paired-transfer pairs to confirm both legs carry identical `metadata.layer.transfer.pair`. Confirm `SELECT count(*) FROM transfer_match_anomalies WHERE reason_code IN ('ambiguous_iban_match', 'ambiguous_reverse_iban', 'ambiguous_amount_match')` returns 0 (the schema migration would have failed otherwise, but assert it for posterity). Confirm `\d transfer_anomaly_reason` shows exactly 5 values. Confirm `\d transactions` shows the new universal index and not the three v1 tier indexes. **[Agent: python-backend]**
