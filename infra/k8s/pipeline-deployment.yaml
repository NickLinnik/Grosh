apiVersion: apps/v1
kind: Deployment
metadata:
  name: grosh-pipeline
  namespace: grosh
  labels:
    app: grosh-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grosh-pipeline
  template:
    metadata:
      labels:
        app: grosh-pipeline
    spec:
      containers:
        - name: pipeline
          image: grosh-consumer:latest
          imagePullPolicy: Never
          command: ["python", "-m", "grosh_pipeline.main"]
          envFrom:
            - secretRef:
                name: grosh-secrets-consumer
          livenessProbe:
            exec:
              command: ["test", "-f", "/tmp/healthy-pipeline"]
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
