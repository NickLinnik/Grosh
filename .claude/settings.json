{
  "extraKnownMarketplaces": {
    "awos-marketplace": {
      "source": {
        "source": "github",
        "repo": "provectus/awos"
      }
    }
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.py) uv run ruff format \"$f\" 2>/dev/null || true;; esac; }",
            "statusMessage": "Formatting Python..."
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.ts|*.tsx) cd services/frontend && npx prettier --write \"$f\" 2>/dev/null || true;; esac; }",
            "statusMessage": "Formatting TypeScript..."
          }
        ]
      }
    ]
  }
}
